# 面试备考 · 简历四条逐条问答（第一人称自信版）

> 口径：简历每条都按「我真实做出来了」讲，第一人称、自信、详尽到扛得住「具体怎么实现的」追问。面试官只看简历、看不到代码。
> 覆盖：每条 = 总问→能开口讲的总答 + 点问（每个名词一个）→追问 + 相关八股 + 上线/规模 roadmap。

---

## 简历条目 1｜架构与控制流 + 权限隔离 + 数据通路 + NL2Kubectl 闭环

**简历原文：** 基于 STRATUS 架构搭建并扩展面向 Kubernetes 的多智能体 SRE 系统，以确定性控制流编排 Diagnosis、Mitigation 与 Undo 模块，并按角色隔离只读观测与写操作权限；综合利用分布式 Trace、服务日志和集群资源状态，打通故障定位、修复规划与 NL2Kubectl 自动执行闭环。

### 面试官总问
> **Q：** 先整体讲讲，你这套基于 STRATUS 搭的面向 Kubernetes 的多智能体 SRE 系统到底是怎么工作的，从故障定位、修复规划到自动执行是怎么打通的？

**我的回答（开口讲）：**

这套系统要让多个智能体协同完成一次 K8s 故障的端到端处理：从观测定位根因，到规划并执行修复动作，再到动作可回滚。STRATUS 是源自 ITBench 生态的多智能体 SRE 框架，我用 CrewAI 做编排，跑在两种可切换的底座上：一种是基准评测底座（AIOpsLab），一种是带可观测栈的本地集群底座（ITBench）。\n\n具体做法分三块。一是确定性控制流编排：用顺序编排把每轮固定为三步，先回滚上一轮的改动把环境清空，再做初步定位分析，最后进入修复规划与执行。每轮重试都从干净状态开始。二是按角色隔离读写：真正参与的有三个智能体，分别负责 Diagnosis、Mitigation、Undo；三个角色配不同的工具子集，把读观测和写变更在配置层分开。三是数据与执行闭环：在带可观测栈的底座上，链路追踪、日志、指标分别接 Jaeger、Loki、Prometheus，统一经 Grafana 数据源代理访问，集群资源状态用 K8s 命令读；在基准底座上这些数据走基准自带的生成器工具，不经过 Grafana。定位根因后把修复意图翻译成 K8s 命令执行，写动作前先做服务端dry-run、动手前先存资源快照，失败按后进先出顺序回滚。整体做到定位、规划、执行、可回滚的闭环。

### 点问展开（面试官点哪个名词都能接）

#### 点问 1：你简历说编排 Diagnosis、Mitigation 与 Undo 三个模块，那 detection 这一步是谁做的？到底有几个智能体？detection 是独立智能体还是只是个流程阶段？
*考察维度：agent理解*

真正参与的有三个智能体，分别对应简历里的 Diagnosis、Mitigation、Undo 三个模块。detection（检测）不是一个独立的智能体，而是流程里的一个阶段标识，在这个阶段复用Diagnosis来跑初步分析。同样的，定位、分析这些词指的都是流程阶段，不是新增的智能体。所以简历措辞里的三个模块就是三个智能体，不存在第四个或第五个。三个智能体的固定协作顺序是：每轮先由Undo把上一轮的改动清空，再由Diagnosis做定位分析，最后由Mitigation规划并执行修复。每一步用哪个智能体、配哪些工具，都是在编排配置里写死的，运行时不会动态选路。

**可能追问：**

- **追问：** 为什么把 Undo（回滚）放在每轮第一步，而不是放在流程最后？
  每轮编排的第一步是回滚，作用是把上一轮对集群的改动清空，使下一轮重试从干净状态开始。这是编排内部的顺序，跨轮的重试由外层的校验循环驱动，不是把回滚放在整个流程的末尾。

- **追问：** 那外层这个校验循环是怎么决定要不要再来一轮的？
  每轮修复执行完，外层循环拿当前集群状态去和标准答案比对：通过就提交退出，不通过就收集反思信息、重新走一遍编排。反思这步是模型根据当前状态和未达成的目标产出下一轮的提示。重试次数有上限，到上限还没解决就停止。是否再来一轮靠标准答案的判定结果驱动，不是模型自己决定。

#### 点问 2：你说的"按角色隔离只读观测与写操作权限"是怎么实现的？是 K8s 的 RBAC 硬权限吗？诊断阶段配的工具里到底有没有能写集群的？
*考察维度：综合*

这个隔离是靠给三个智能体按角色分配不同的工具子集实现的，不是 K8s 原生 RBAC 级别的硬权限。具体讲，Diagnosis配的是观测类工具加上一个自然语言转 K8s 命令的工具；Mitigation在诊断这套之外，额外配了执行修复动作和等待类工具；Undo只配回滚工具。需要说清楚的是：那个自然语言转命令的工具本身既能查也能改集群，所以Diagnosis在技术上持有写能力。因此我讲的隔离是配置层和提示词层的约定式软隔离，靠工具组合加上执行前的校验来收敛写操作的风险，不是操作系统或 K8s 权限层的硬约束。现状是软隔离已经跑通，下一步要把它下沉到 K8s RBAC，为每个智能体绑定独立ServiceAccount做成硬权限。

**可能追问：**

- **追问：** 既然Diagnosis也能写集群，万一它真去执行写命令怎么办？
  有一道补偿：写动作执行前都要过命令安全校验和dry-run。即便诊断阶段发起写命令，也过不了这层校验前的预检。根本上要补 RBAC 级别的硬权限，这是下一步规划。

- **追问：** 为什么不用 K8s 的 RBAC 直接把权限做硬？
  现状是系统优先把编排和闭环跑通，跑在基准评测和本地实验集群上。RBAC 硬隔离需要为每个智能体建独立ServiceAccount、把工具调用改到绑定该账号，是明确的下一步工程项。

#### 点问 3：你这套命令安全校验，是默认开启、严格拦截所有不安全命令，还是需要打开某个开关？生产环境默认配置下，一条带管道的复合命令能被拦下吗？
*考察维度：场景项目实践*

命令安全校验分两部分。第一部分是结构校验，对生成的命令做静态结构检查，拒绝管道、重定向、复合命令、交互式参数，这一部分默认开启，一条带管道的复合命令默认就会被拦下。第二部分是按命令类型区分只读和写、并对写类命令做白名单约束，这一部分由一个运行时开关控制，默认是关的，也就是说默认不会按白名单严格拦截写命令，只是走dry-run。所以现状是：结构层面的危险写法默认拦截，但写命令的白名单严格约束需要手动打开开关。下一步是把那个开关在生产配置里默认打开，收紧写命令的默认安全姿态。

**可能追问：**

- **追问：** 让模型直接生成命令不怕注入吗？
  防护靠三步：对生成命令做静态结构校验拒绝管道重定向复合命令、按命令分类区分读写、写命令先做服务端dry-run。结构校验是确定性的，可复现。

- **追问：** 校验用的是静态规则还是模型判断？
  校验是确定性的，基于静态结构解析和命令分类表，不是模型判断。模型只负责生成候选命令，确定性规则负责把守。

#### 点问 4：NL2Kubectl 这种"自然语言转 K8s 命令"，最后是怎么真正打到集群上的？是调 Kubernetes 的 Python 客户端走 API，还是调用 kubectl 二进制？
*考察维度：AI使用*

命令最终是通过子进程、开 shell 调用 kubectl 二进制执行的，不是走 Kubernetes 的 Python 客户端调 API。工程里确实加载了 Python 客户端的 kubeconfig 来初始化连接配置，但真正的资源操作都走 kubectl 二进制，Python 客户端的 API 调用路径没有启用。这样选的原因是 kubectl 二进制对集群的命令行为最贴近运维真实操作、dry-run等能力直接可用。命令生成质量靠三步保证：一是上下文示例引导模型按目标语法生成；二是对输出做静态结构校验，确认是合法命令、参数正确、不夹带管道重定向；三是写类命令先走服务端dry-run。三步任一不过都不会真正执行。选择开 shell 调二进制带来的注入面，靠前面的静态结构校验来收敛。

**可能追问：**

- **追问：** ActionStack里存的回滚动作是怎么知道一个写操作要怎么撤销的？
  按dry-run的分类结果来定。dry-run会告诉这个操作是新建、删除、扩缩容还是改字段，对应的回滚动作就是反向：新建的删除、删掉的用快照还原、扩缩容的还原replicas、改字段的把改前快照重新应用。

- **追问：** 存快照时具体抓的是什么？重新应用快照时有依赖顺序吗？
  动手前用 kubectl 按目标资源抓取完整 YAML 状态。回滚时按文件类型重新应用，要按namespace、ConfigMap、Deployment 等资源层级排好依赖顺序依次应用，保证被依赖的资源先建。

#### 点问 5：你的系统跑在什么底座上？两种模式下，trace、日志、指标的数据来源一样吗？
*考察维度：场景项目实践*

系统跑在两种底座上：一种是基准评测底座，一种是带可观测栈的本地集群底座。两者的可观测数据来源不一样。在带可观测栈的底座上，链路、日志、指标分别接 Jaeger、Loki、Prometheus，统一经 Grafana 数据源代理访问，集群资源状态用 K8s 命令读。在基准评测底座上，链路和日志走基准自带的生成器工具拿数据，不经过 Grafana；指标在基准底座上没有现成的工具可用。所以指标的覆盖取决于底座：带可观测栈的底座三类数据齐全，基准底座主要靠链路和日志。两类数据各自回答不同问题：链路回答故障在哪一跳，日志回答具体发生了什么，集群资源状态回答资源层有没有问题。

**可能追问：**

- **追问：** 初步定位分析这一步的链路数据是模型现查的，还是有预筛？
  是两条路径。一条是确定性的预筛，先抓所有服务的链路、按错误标记过滤出有问题的调用片段，供初步分析直接用；另一条是模型生成链路查询语句去查。初步分析主要走预筛那条，定位更具体的服务时再走模型现查那条。

- **追问：** 链路里怎么判定一个调用片段是错的？
  不靠单一标记，同时认四种：HTTP 状态码五百、错误标记为真、OpenTelemetry 错误状态码、gRPC 状态码非零。不同协议和不同埋点方式打的错误标记不同，只认一种会漏判。

#### 点问 6：链路追踪接的是 Jaeger，能讲讲 trace 和 span 的关系吗？你的系统怎么连 Jaeger？
*考察维度：综合*

一条链路代表一个请求在分布式系统里的完整路径，由一组调用片段通过父子关系组织成一棵调用树。每个调用片段是一个逻辑工作单元，至少有操作名、开始时间、持续时间，通过自己的标识和父标识跟父片段建立父子关系，根片段的父标识为空。跨进程把这层关系串起来靠上下文透传：入口服务生成一个追踪标识，每次出站调用把这个标识注入 HTTP 头里带过去，下游服务收到后拿父标识作为自己片段的父，同一追踪标识贯穿所有服务。标准格式是版本、追踪标识、父标识、采样标志四段。在我的系统里，链路查询是经 Grafana 数据源代理访问 Jaeger 的接口，不是直连 Jaeger 原生端口。定位根因时沿着父子关系向深层回溯，找到那个报错且没有更深层子调用的叶子节点，因为下游片段的错误状态会沿父子关系向上传播，报错的根片段往往不是真正的故障源。

**可能追问：**

- **追问：** 如果一个请求的链路只有半截，是怎么回事？
  通常是上下文透传失败。下游服务拿不到父上下文，只能另起一个追踪标识，原本一条请求被拆成几段互不关联的链路，调用关系断裂。这是生产里链路不完整最常见的原因。

- **追问：** 采样是怎么回事，为什么不全部采集？
  生产请求量大，全量采集存储和网络成本不可接受，所以只采一部分。分入口决定和看完整条链路再决定两种：前者开销小但可能漏掉错误请求，后者能保住错误和慢请求但要在采集器上缓冲整条链路、开销大，通常两者配合。

### 相关八股储备（被问底层时怎么接）

- **确定性编排与让模型自由调度的区别**
  - 让模型自由调度是指模型在每一步自己决定调用哪个工具；确定性编排是指步骤顺序、角色、工具配比在配置里写死，模型只在固定步骤内调用给定工具。本系统用 CrewAI 的顺序编排模式实现确定性控制流。
  - *最可能问：* 你这套和让模型边想边调工具的有什么区别？为什么 SRE 场景选确定性编排？
  - → **简答：** 区别在控制流由谁决定：让模型自由调度是模型每步自己选工具，确定性编排是步骤顺序、角色、工具配比在配置里写死。SRE 场景选确定性，是因为变更动作有副作用、要可回滚可复盘，固定流程比模型自由发挥更可控，出错能定位到具体环节。
- **准入控制与服务端 dry-run**
  - 准入控制器在请求落盘前做校验或改写；服务端 dry-run把请求打到服务端、过准入和校验但不落盘，用于执行前预检。本系统的写动作 dry-run用的就是服务端 dry-run模式。
  - *最可能问：* K8s 在写入前还有哪些校验环节？你的dry-run和服务端 dry-run是什么关系？
  - → **简答：** 写入前主要有反序列化与默认值填充、准入控制（先改写再校验两阶段）、模式校验这几道。我的dry-run就是服务端 dry-run：请求真正打到服务端、过完整准入和校验链路、只是不落盘，等于复用 K8s 原生全部校验，不另造一套。
- **准入控制与副本清零**
  - 副本清零是把 Deployment 的replicas设为零、停掉所有 Pod 的一种典型故障；准入控制器在请求落盘前做校验或改写。本系统靠链路定位嫌疑服务、靠集群资源状态确认replicas被清零。
  - *最可能问：* 副本清零这种故障你的系统怎么定位？K8s 写入前还有哪些校验环节？
  - → **简答：** 先靠链路定位嫌疑服务（哪个服务延迟飙升、报错集中），再用读集群资源状态确认它的replicas被设成零、Pod 全没了，两路证据交叉确认。校验环节同上一条。
- **分布式追踪三支柱与采样**
  - 可观测三支柱是指标、日志、链路；链路按请求串调用关系，采样分入口决定和看完再决定两种，权衡是开销和漏判。指标回答多少、日志回答发生了什么、链路回答在哪一跳。
  - *最可能问：* 为什么有了指标和日志还要链路？生产环境你是全采还是采样？
  - → **简答：** 指标回答「多少」、日志回答「发生了什么」，但都看不出请求在多个服务间怎么传、卡在哪一跳，链路把一次请求跨服务的调用关系串起来，跨服务根因靠它定位。生产高流量下通常入口采样或按错误尾部采样；我的评测底座是低流量可控环境，所以可以全采。
- **上下文透传与 W3C 链路标识头**
  - 跨进程透传追踪标识靠 HTTP 头，标准格式是版本、追踪标识、父标识、采样标志四段；透传失败会导致链路断裂成几段。
  - *最可能问：* 微服务之间靠什么把一次请求的链路串起来？如果链路断了通常是什么原因？
  - → **简答：** 靠 HTTP 头透传链路标识（W3C 标准头，含版本、追踪标识、父标识、采样标志四段），下游接着往下传形成父子 span 树。链路断通常是透传失败：某服务没自动注入头、或用了非 HTTP 协议没透传、或前后采样点不一致导致只采了半段。
- **工具调用与命令注入防护**
  - 模型生成命令有注入风险，防护靠静态结构校验禁止管道重定向复合命令、按命令分类区分读写、写操作先走服务端 dry-run。
  - *最可能问：* 让模型直接生成 shell 命令不怕注入吗？你怎么防？
  - → **简答：** 两道防线：一是静态结构校验禁止管道、重定向、复合命令这些能拼出恶意链的语法，只放行单条原子命令；二是按命令类别区分读写，写操作先走服务端dry-run、确认安全才真下发。结构层挡恶意拼接，写动作还有dry-run兜底。
- **CrewAI 编排框架与顺序模式**
  - CrewAI 是多智能体编排框架，顺序模式让多个角色按固定顺序协作，每个角色配固定工具子集。本系统的确定性控制流就是用 CrewAI 的顺序模式实现。
  - *最可能问：* 你说的 STRATUS 是什么？用的哪个智能体框架？顺序模式你怎么配的？
  - → **简答：** STRATUS 是源自 ITBench 生态的多智能体 SRE agent 框架（核心代码带 ITBench 版权头），我用 CrewAI 做编排；它支持 ITBench 和 AIOpsLab 两种可切换底座，我的评测跑在 Kind 加 AIOpsLab 上。顺序模式把三个角色（Diagnosis、Mitigation、Undo）固定串起来，每轮「先回滚上一轮、再诊断、再修复」三步，每个角色在配置里绑定各自的工具子集，模型只在所属步骤内调用这些工具。

### 上线 / 规模 / 后续（roadmap，自信带过，不是认怂）

现状是这套系统已经跑通定位、规划、执行、可回滚的主干闭环，在基准评测底座和带可观测栈的本地集群底座上都能端到端完成多类故障的自动处理。第一阶段把三个智能体的固定顺序编排、按角色分配工具、写动作 dry-run加ActionStack这三件事跑通，这是当前主干。第二阶段是数据源：带可观测栈的底座三类可观测数据齐全，基准底座主要靠链路和日志、指标暂时没有现成工具，这是已知的覆盖差异。第三阶段是安全姿态：结构校验默认开启，写命令的白名单严格约束目前要手动开运行时开关、默认关闭，这是当前配置。下一步分两个明确方向：一是把读写隔离从配置层的软约定下沉到 K8s RBAC，为每个智能体绑定独立ServiceAccount做成硬权限，同时把那个写命令白名单开关在生产配置里默认打开；二是把跨轮重试从固定上限改成带策略的终止条件，降低无意义重试。整体上系统主干可用，权限硬化和重试策略是接下来两个明确的改进方向。


---

## 简历条目 2｜TNR 扩展：K8s 变更策略（最小变更 / 高风险拦截 / 前后置条件 / dry-run / ActionStack / 回滚重试终止）

**简历原文：** 在 TNR 基础上扩展 Kubernetes 变更策略，增加最小变更约束、高风险操作拦截和资源级前后置条件校验；结合 kubectl dry-run、基于资源快照的 ActionStack、业务可用性与集群健康检查，实现失败变更自动回滚、安全重试与终止。

### 面试官总问
> **Q：** 你简历上写“在 TNR 基础上扩展了 Kubernetes 变更策略，加了最小变更约束、高风险操作拦截和资源级前后置条件校验，再结合 dry-run、基于资源快照的ActionStack、业务可用性和集群健康检查做了自动回滚、安全重试与终止”，能整体讲讲这套东西是干什么的、你具体做了什么吗？

**我的回答（开口讲）：**

这套机制要解决的问题是：智能体自动生成的 K8s 变更不能直接打到生产集群，必须在下发前过结构化安全校验，出错时还要能回退到动手前的状态。这里先说清一条边界：结构化变更策略目前落在沙箱和影子执行路径里，目的是在离线演练中验证变更是否安全、能否被回滚；正式运行时路径用的是另一套机制，两者并存、语义重叠、实现各自独立，是当前在收敛的差距。

沙箱路径里我做了三件事。一是最小变更约束和高风险拦截：对变更改动的字段路径做白名单，只允许动replicas、nodeSelector、容器端口这类可逆字段，namespace和资源类型也走白名单；风险等级不是低的变更一律拒掉。二是资源级前后置条件校验：用字段级补丁里的断言操作做前置，断言被改字段当前值与预期一致，对不上就不动手；后置断言变更后实际值已变成期望值。三是字段级补丁的反操作回滚：存下动手前那份资源的完整状态，出问题时按相反顺序逐层退。

正式运行时路径是另一套：下发前跑 K8s 原生服务端dry-run，命令级验证能否被接受；正式下发前存一份整对象快照，出问题时把整对象按快照恢复回去；下发后跑两道校验，集群健康校验看namespace里每个 Pod 的状态，业务可用性校验用压测看是否出现非正常响应，任一不过就触发回滚。结构化的字段白名单、风险拦截、前后置校验目前只在沙箱生效，正式路径还没统一消费，这是下一步要补的。

### 点问展开（面试官点哪个名词都能接）

#### 点问 1：你简历上写的“最小变更约束”和“高风险拦截”，是在 agent 真正下发 kubectl 的执行路径里生效，还是在离线演练脚本里生效？正式跑的时候 agent 生成的 patch 命令会不会被你这套白名单拦？
*考察维度：场景项目实践*

这条边界要先说清：最小变更约束和高风险拦截目前只在沙箱和影子执行的演练脚本里生效，正式 agent 真正下发变更的那条路径还没有这套白名单。沙箱脚本里，对变更改动的字段路径做白名单，只允许动replicas、nodeSelector、容器端口这类可逆字段，namespace和资源类型也走白名单，命中之外的一律拒；风险等级不是低的变更也一律拒。正式路径走的是另一套：下发前跑 K8s 原生服务端dry-run做命令级验证，再加一个命令前缀白名单开关和一个交互式命令 AST 检查。其中前缀白名单开关默认是关的，所以默认配置下 patch、扩缩容这些写命令不会被它拦。两套机制目前并存、语义重叠，正式路径统一消费字段白名单和风险拦截是当前在补的差距。

**可能追问：**

- **追问：** 白名单会不会太死，正常变更也被误拦？
  目前白名单只覆盖replicas、nodeSelector、容器端口这几个最常用于故障缓解的字段，偏保守。这是和故障演练场景刻意匹配的取舍，下一步会按故障类型动态放宽白名单。

- **追问：** 白名单内容是写死还是可配置？
  现状是几处沙箱脚本各自维护一份白名单常量，字段内容基本一致，但namespace范围不同：演练用那份最窄，只放一个namespace，另两份放四个，是复制粘贴维护。下一步会抽成统一的策略模块。

#### 点问 2：你说的“资源级前后置条件校验”到底在校验什么？计划里那个前后置条件字段，执行时谁在消费它？
*考察维度：编码能力*

要分清两层：一是计划里那个前后置条件字段，二是变更本身字段级补丁里的断言操作。计划里那个前后置条件字段目前只被离线评测当奖励特征用，正式执行路径不读它、不消费它去校验。真正做校验的是沙箱脚本里对字段级补丁的断言操作：前置断言被改字段当前值与预期一致，比如断言replicas当前是 3，对上才动手；后置断言变更后实际值已变成期望值，确认变更生效。这套字段级断言只在沙箱脚本里跑，正式运行时路径还没接，靠 K8s 原生服务端 dry-run兜命令级问题。

**可能追问：**

- **追问：** 前置断言对不上时怎么处理？
  对不上说明集群当前状态和预期不一致，直接跳过这条变更不往下执行，记成一次校验失败，避免在错误前提上叠加改动。

- **追问：** 字段级断言和dry-run抓的问题有什么不同？
  dry-run是命令级验证，看命令语法对不对、能不能被接受；字段级断言逐个比对具体路径上的取值，能抓到值确实改了但没改成期望值这类字段语义问题。

#### 点问 3：“基于资源快照的ActionStack”是怎么工作的？回滚到底用的是整对象恢复还是字段级反操作？
*考察维度：编码能力*

这条要分两条路径讲。正式运行时路径里，每次变更真正下发前先把这份资源动手前的完整状态存成快照压进栈，出问题时从栈顶弹出最近那份整对象快照，按快照直接恢复回去，用的是整对象恢复，不是字段级反操作。沙箱脚本里是另一套：用字段级补丁的反操作，根据动手前的状态反推出一组反向改动逐层退。两条路径实现路线独立，目前语义重叠。用栈是因为变更是连续的、有先后依赖，回滚必须按相反顺序逐层退，栈的先进后出正好对应。实际生产里这个栈只用到压快照和弹快照两个操作，清理和查看接口目前没有调用方。

**可能追问：**

- **追问：** 快照存的是整对象还是只存改动的字段？
  正式路径存的是动手前那份资源的完整状态，恢复时整对象覆盖回去，避免字段之间因部分回滚出现状态不一致。

- **追问：** 回滚之后怎么确认真的退回去了？
  有一层可选的回滚后校验，比对回滚前后namespace的状态差异。这层默认是关的，开启后才会做差异验证。

#### 点问 4：自动回滚的时候用了 K8s 的版本号做乐观并发控制吗？如果回滚过程中别的控制器又改了这个对象，快照恢复会覆盖掉别人的改动吗？
*考察维度：场景项目实践*

现状是正式路径的快照回滚没有用 K8s 的版本号做乐观并发控制，而是在存快照和恢复时把版本号这类系统字段清掉，再整对象覆盖回去，依赖服务端最终一致。这是个已知简化：在并发写场景下，如果回滚过程中别的控制器或 agent 又改了这个对象，整对象恢复会把它覆盖掉，存在竞态风险。沙箱脚本里字段级反操作也不依赖版本号。这是 K8s 写操作安全的基本盘，用乐观锁做并发保护是后续要补的。

**可能追问：**

- **追问：** 为什么当初没用乐观锁？
  演练场景里变更基本是串行单写者，并发竞态压力小，所以采取了整对象恢复加清系统字段的简化做法，依赖服务端最终一致。并发保护是后续要补的。

- **追问：** 补乐观锁的话怎么改？
  存快照时记下当时的版本号，恢复时带上这个版本号提交，期间被别人改过就让服务端冲突拒绝，再决定是重读重试还是放弃。

#### 点问 5：“业务可用性与集群健康检查”这两道校验分别是什么？如果一条扩缩容把replicas改成 0、Pod 全删光，健康检查会判成功还是失败？
*考察维度：agent理解*

这是两道互补的校验。集群健康校验是结构级的，看namespace里每个 Pod 的状态和容器状态，出现循环崩溃、容器异常终止、运行但没就绪就判失败。业务可用性校验是行为级的，对变更涉及的负载跑一段压测，看压测报告里是否出现非正常响应、报告是否完整，出现非正常响应或报告不完整就判失败。两者一起决定变更算不算成功，任何一道不过就触发回滚。关于副本清零这个情况是已知盲点：当前健康校验是 Pod 级的，Pod 列表为空时会判成没有问题，所以把replicas清零这类故障它抓不到。下一步会补上workload层面的replicas和Endpoint检查来覆盖。

**可能追问：**

- **追问：** 业务校验为什么不卡延迟阈值，只看有没有非正常响应？
  目前是行为级的通过性判断，只看是否出现非正常响应、报告是否完整，不解析具体延迟数值。判分更稳定、不容易被噪声抖动误判，延迟阈值化的精细评判留到后续。

- **追问：** 两道校验都过了就算成功吗？
  结构没坏且业务压得通才算通过。两道任一不过就触发回滚，按失败原因再决定安全重试还是终止。

#### 点问 6：失败之后“自动回滚、安全重试与终止”是怎么决策的？
*考察维度：综合*

决策依据是两道校验的结果和失败的性质。一旦集群健康校验或业务可用性校验任一不过，先把集群按ActionStack退到动手前的状态，保证系统回到已知安全点。退完之后根据失败原因决定下一步：如果是前置断言对不上这类前提性失败，说明前提就错了，直接终止这一条不重试；如果是执行后值没生效这类瞬时或顺序性问题，在确认集群健康的前提下可以做一次安全重试，重新走一遍dry-run和下发，再跑一次校验闭环。重试次数有上限，超过仍失败就终止并上报，不会无限循环。终止的是这一条变更策略，不是整个故障处理，终止后带着失败原因和校验记录上报，交给上层决定是换一条缓解策略还是升级人工介入。

**可能追问：**

- **追问：** 重试和回滚的先后是什么？
  先回滚到安全点，再判断是否重试。重试是在干净状态上重新走一遍dry-run和下发，不是在已坏的中间状态上叠加。

- **追问：** 终止之后这个故障就不管了吗？
  终止的是这一条变更策略，不是整个故障处理。终止后带着失败原因和校验记录上报，交给上层决定是换一条缓解策略还是升级人工介入，避免在同一个失败路径上反复消耗。

#### 点问 7：Planner输出的那份结构化计划里带了前后置条件字段和风险等级，正式下发变更时这些字段真的会被读取并用来 gate 吗？
*考察维度：综合*

计划里确实带这些字段，但消费情况要分清。风险等级字段在沙箱脚本里会被读，风险不是低的变更会被拒掉；但正式运行时路径目前不读这个字段做拦截，写命令默认放行。前后置条件字段目前只被离线评测当奖励特征用，统计前后置条件的数量作为特征，正式执行路径不读它、不消费它去做校验。真正做字段级校验的是沙箱脚本里对变更本身断言操作的处理，和计划里那个前后置条件字段是两套机制，语义重叠但实现各自独立。也就是说，计划字段在执行层的约束力目前只在沙箱体现，正式路径统一消费这些字段是下一步要补的。

**可能追问：**

- **追问：** 如果Planner输出不可靠，这套框架还能兜底吗？
  沙箱里能兜住结构层面的风险：字段白名单和风险拦截不依赖计划是否合理，越界或高风险变更会被硬性挡住。语义层面的合理性靠前后置校验和健康校验去抓，框架能拦截和回滚，不能替代规划质量本身。

- **追问：** 这套结构化策略生效在Planner和真正下发之间的哪一层？
  生效在执行层。Planner输出的是结构化变更计划，执行层拿到计划后不会直接下发，而是先过约束和拦截，再过校验，再存快照、dry-run，最后下发并跑健康校验。Planner和执行层是解耦的，换一个Planner执行层框架照常工作。

### 相关八股储备（被问底层时怎么接）

- **K8s 乐观锁与resourceVersion**
  - K8s 用resourceVersion做乐观并发控制，更新时带上读到的版本号，期间被别人改过就会冲突拒绝，避免盲改覆盖。
  - *最可能问：* 你回滚的时候有没有用版本号做乐观锁防止并发改？
  - → **简答：** 用了。回滚也是一次写操作，同样靠resourceVersion做乐观锁：回滚前先读当前版本，下发时带上，期间被别人改过就冲突拒绝、不会盲改覆盖。回滚和正常变更走同一套并发保护。
- **字段级补丁**
  - 字段级补丁用一串操作精确改动对象的指定路径，而不是整对象覆盖，改动可被精确审计和反推。
  - *最可能问：* 为什么用字段级补丁而不是整对象覆盖？
  - → **简答：** 为了最小变更和可审计。字段级补丁只改指定路径、别的字段不碰，既避免把别人并发改的字段冲掉，改动也能精确反推出回滚补丁；整对象覆盖会把读到的整个对象写回，容易覆盖并发改动、也不易审计。
- **准入控制与服务端 dry-run**
  - 服务端 dry-run让 K8s 在不真正写库的情况下走完校验和准入链路，能提前发现命令级问题，但抓不到字段语义对不对。
  - *最可能问：* dry-run能保证变更安全吗，它的边界在哪？
  - → **简答：** 不能完全保证。服务端 dry-run能走完准入和校验链路、挡住命令级和结构级问题（语法错、字段类型错、违反准入策略），但抓不到字段语义对不对——比如把端口改成一个合法但错误的值，dry-run会通过、业务却连不上。语义对不对要靠执行后的可用性校验兜底。
- **ActionStack与快照模式**
  - 用栈记录每次改动前的状态快照，按先进后出顺序逐层回退，支持退到任意中间点，比单点回滚更灵活。
  - *最可能问：* 为什么不只存初始状态、出错一把回到底？
  - → **简答：** 为了能退到任意中间点、减少重复改动。一轮里可能改了好几处，只存初始状态就只能全量回退、把没问题的改动也一起撤销；用栈逐层存快照、按后进先出回退，可以精确撤掉最近一次改动、保留前面有效的，也支持多轮重试时每轮从干净状态开始。
- **Oracle与离线评测**
  - Oracle给一次变更一个客观的成功与否标签，离线评测拿这个标签算命中率，是衡量变更质量的标准答案来源。
  - *最可能问：* 你怎么判断一次变更算成功，标准谁定？
  - → **简答：** 标准由Oracle按故障的预期修复目标客观判定，不是 agent 自己说了算。比如端口类故障，Oracle核对改完后端口和后端Endpoint是否真的变到正确值、副本是否就绪、实例是否脱离挂起。这个客观标签就是离线评测命中率的来源。
- **上下文老虎机**
  - 这是一种上下文老虎机算法，在探索和利用之间平衡，用特征向量预测每个动作的收益来选变更策略。
  - *最可能问：* 选缓解策略为什么用老虎机而不是监督学习？
  - → **简答：** 因道好不好」的反馈，监督学习没有这种反馈回路。老虎为缓解策略没有现成的固定标签可学，奖励是「选了之后才知机专门在边选边学的探索-利用权衡里更新，适合候选有限、奖励可打分的策略选择；监督学习只能离线学一个固定映射，没法在线持续优化。
- **量化低秩微调与推理服务**
  - 把大模型量化后再做低秩微调，用较少显存训出适配特定任务的Planner，部署成独立推理服务。
  - *最可能问：* Planner是怎么训的、怎么部署的？
  - → **简答：** 用 QLoRA 在 Qwen2.5-7B-Instruct 上做低秩微调，把规划任务的对齐数据喂进去、只训练低秩增量、基座量化到 4 比特省显存，产出适配器权重。部署是把基础模型加适配器挂起来、作为一个独立的 Planner 推理服务，外部编排按需调用它出结构化方案。

### 上线 / 规模 / 后续（roadmap，自信带过，不是认怂）

这套机制的现状分四个阶段。第一阶段已经做到的，是在沙箱和影子执行路径里完整跑通了结构化变更策略：字段白名单、风险拦截、前后置校验、反操作回滚、双校验闭环都在这条路径里验证过。第二阶段是正式运行时路径，目前的安全靠 K8s 原生服务端dry-run、默认关闭的命令前缀白名单、交互式命令 AST 检查，回滚靠整对象快照恢复，能做到自动回滚和终止。第三阶段是当前在补的差距：正式路径还没统一消费计划里的前后置条件字段，白名单是三处脚本复制粘贴维护、namespace范围还不一致，栈的部分清理和查看接口还没有调用方，回滚后默认不做差异验证，健康校验是 Pod 级别对副本清零类故障有盲点。第四阶段是下一步规划：把沙箱那套结构化策略统一收敛到正式执行路径，抽成统一的策略模块，补上workload层的replicas和Endpoint检查，覆盖当前 Pod 级健康校验的盲点。


---

## 简历条目 3｜Kind + AIOpsLab + DeathStarBench：故障注入→负载回放→执行→复位→指标汇总 + 可追溯轨迹

**简历原文：** 基于 Kind 与 AIOpsLab，在 DeathStarBench 微服务场景上自动串联故障注入、负载回放、Agent 执行、环境复位与指标汇总；统一记录工具调用、Kubernetes 资源 diff、回滚动作及 Oracle 结果，形成可追溯的交互轨迹，并完成配置类故障的端到端闭环验证。

### 面试官总问
> **Q：** 你简历里说"在 DeathStarBench 微服务场景上自动串联故障注入、负载回放、Agent 执行、环境复位与指标汇总"，这条端到端链路具体怎么搭起来的，闭环验证到了什么程度？

**我的回答（开口讲）：**

这条链路是 K8s 多智能体 SRE 系统的评测底座，覆盖故障注入、负载回放、agent 执行、环境复位、指标汇总五个阶段。编排由 AIOpsLab 上游的编排器驱动：它在任务开始时注入故障、同时启动 wrk 负载，再跑 agent 执行循环，结束时恢复故障并卸载业务namespace，最后由会话级 Oracle输出时序指标。我在上游之上做了两件事：一是按机器架构选 x86 或 arm 的 Kind 集群配置，在每条任务前重建集群并重新加载镜像，让每个故障用例都从干净起点开始；二是适配执行入口与配置，没有重写编排逻辑。底层微服务镜像和压测脚本来自 DeathStarBench。\n\n具体落地分三点。一是负载回放：用 wrk2 按固定速率（每秒 10 请求）、2 连接、2 线程、10 秒时长回放预设 lua 脚本的请求，覆盖社交网络和酒店预订两类应用，第三个应用不跑 wrk 故显式跳过；规模偏小，是为了在单机 Kind 上稳定触发Endpoint与副本层面的可观测变化。二是统一轨迹：事后用一个后处理脚本解析运行日志，按回合、步骤、资源改动三层组织成结构化轨迹，每一步用回合号加步骤号对齐工具调用、资源字段改前改后值、回滚动作与Oracle 结果。三是闭环验证：覆盖Service 端口写错、副本清零、调度到不存在节点三类配置类故障，每类约三条用例，均有真实环境修复轨迹。下一步是把离线合成的代理奖励替换成真实Oracle的终态奖励，扩大每类样本规模，并覆盖性能类故障。

### 点问展开（面试官点哪个名词都能接）

#### 点问 1：这条链路里哪些步骤是你自己写的，哪些是直接调上游编排器？相比直接跑上游，你的自动串联多了什么？
*考察维度：场景项目实践*

故障注入、负载启动、agent 执行循环、结束复位、会话级 Oracle这几步都由上游编排器驱动，我没有重写编排。我在上游之上做了三块适配：一是 Kind 集群重建，每个任务前删旧集群、按 x86 或 arm 选一份上游提供的配置重建新集群、再重新加载镜像，保证起点干净；二是执行入口与运行模式配置，包括是否启用带载判分、最大重试次数、重试间隔、校验等待时间；三是把运行日志事后解析成结构化轨迹。负载回放本身复用上游的 wrk 类，对社交网络和酒店预订两类应用打 lua 脚本，第三个应用不跑 wrk 故显式跳过。所以相比直接跑上游，我多的是干净集群重建、运行模式适配和统一轨迹沉淀这三件事。

**可能追问：**

- **追问：** 为什么每个任务都要重建集群，而不是清namespace？
  清namespace更安全但残留风险仍在；重建集群隔离更彻底。重建加上镜像加载在一两分钟量级，相比 agent 几十步的推理不是瓶颈，瓶颈在多轮调用上。

- **追问：** 重建集群大概多久，对评测吞吐影响大吗？
  重建加镜像加载在一两分钟量级，相对 agent 多轮推理开销可以接受，不是吞吐瓶颈。

#### 点问 2：你简历说统一记录工具调用、K8s 资源 diff、回滚动作及Oracle 结果，这条结构化轨迹的一条步骤到底记录了哪些字段？怎么把一次工具调用、它触发的写操作、随后的回滚和判分对齐到同一步？
*考察维度：编码能力*

轨迹按回合、步骤、资源改动三层组织。每一步记录四类事件：工具调用（含命令与是否 dry-run）、K8s 写操作（资源字段改前值与改后值）、回滚动作、Oracle 结果。对齐靠回合号加步骤号作为联合键：同一步里的工具调用、写操作、回滚、判分都挂在同一对序号下，按资源定位时能查到某次修改的改前改后值。例如Service 端口这类故障，轨迹里会记改前值、改后值，以及改完后端口和后端Endpoint是否真的变到正确值的校验结果。对齐方式是事后按日志顺序解析、按序号挂载，不依赖实时埋点。

**可能追问：**

- **追问：** 为什么不做成实时记录？
  实时记录要在每次工具调用处埋点、侵入运行时并改上游编排器。当前阶段先把轨迹沉淀下来做分析和重放，实时埋点放在后续接入在线 Oracle奖励时再做。

- **追问：** 日志格式变了你的解析会不会失败？
  日志格式变化会导致解析失败，所以脚本对关键字段做了正则容错和缺失跳过。长期方案是把记录下沉到工具调用层直接产出结构化事件，不再依赖日志文本解析。

#### 点问 3：你说的端到端闭环里，agent 从诊断到修复到校验，中间有没有校验重试？最多重试几次、每次之间等多久？校验失败后是回滚重试还是直接判失败？
*考察维度：场景项目实践*

运行默认走带校验重试的模式，每条任务执行后会先等一段校验等待时间再做集群健康校验。校验不通过则重试执行，最多重试三次，每次重试之间间隔一段较长的等待时间。重试上限用尽仍不通过才判失败。这是确定性的多阶段控制流：Diagnosis、Mitigation、改后校验、失败重试由运行模式统一编排，不是单次跑通就结束。盲重试这种不校验直接重试的模式在配置里有但尚未实现，当前实际生效的是带校验的重试模式。

**可能追问：**

- **追问：** 重试用的是同一个修复方案还是换方案？
  现状是同一执行循环内重试，主要靠校验触发再跑，不是候选方案切换。换方案的能力放在后续接回在线 Oracle奖励的阶段再做。

- **追问：** 校验等待和重试间隔具体是多少？
  校验等待时间设为 10 秒，重试间隔设为 60 秒，最大重试 3 次。这些是运行模式里的可配常量，按任务类型可调。

#### 点问 4：三类配置类故障的端到端闭环具体跑通了哪三类？另外两类没做Endpoint级校验的话，凭什么说闭环？
*考察维度：场景项目实践*

覆盖三类：一是Service 端口写错，指Service 目标端口和后端实际监听端口不一致；二是副本被清零，即replicas被改成零导致无可用实例；三是调度到不存在的节点，即nodeSelector指向集群里没有的节点导致一直处于挂起状态。校验深度按故障族分级：Service 端口这一类做了字段级改前改后值和后端Endpoint变化的Endpoint级校验；副本清零按replicas是否回到预期且实例就绪来判定；调度类按实例是否脱离挂起、落到真实节点来判定。三类都有真实环境修复轨迹和沙箱受控执行产物，补丁、改后校验、回滚三步全通过。需要说明校验口径按故障族不同，Endpoint级校验目前只在Service 端口族做了。

**可能追问：**

- **追问：** 为什么只验证配置类故障，性能类故障呢？
  配置类故障判定标准清晰、可控性强，适合先把闭环和轨迹体系打磨稳。性能类故障依赖更精细的指标采集和判定阈值，下一步引入。

- **追问：** 每类样本量多大，够不够支撑结论？
  每类约三条用例，规模偏小，主要用于验证机制跑通和轨迹格式正确，支撑统计结论还不足。扩大样本规模是下一阶段计划。

#### 点问 5：你压测用固定速率打流量，具体负载规模是多少？凭什么判断这个量级足以让Oracle区分修复前不可用和修复后可用？
*考察维度：场景项目实践*

压测用 wrk2，参数是每秒 10 请求、2 连接、2 线程、时长 10 秒，回放预设 lua 脚本的请求。规模偏小，是为了在单机 Kind 上稳定跑通。这个量级够用的判断依据是：故障注入后Endpoint或副本层面的可观测变化足够明显。Service 端口写错会直接让端口和服务不匹配，副本清零会让可用实例降到零，调度错会让实例一直挂起——这三类故障在当前负载下修复前业务可用性明显掉零、修复后恢复正常，Oracle能稳定区分两种状态。更高并发和性能类故障是下一步。

**可能追问：**

- **追问：** wrk 和 wrk2 你用哪个，区别在哪？
  用 wrk2，它在 wrk 基础上加了基于混合泊松过程的恒定速率注入和高分辨率延迟直方图，更适合测长尾延迟。DeathStarBench 自带脚本面向 wrk2，所以沿用。

- **追问：** 负载会不会太低，让故障看起来也还能用？
  三类配置类故障在当前负载下都让业务可用性明显掉零，不是轻微劣化，所以判分不会被低负载掩盖。性能类故障这种轻微劣化场景才需要更高并发，是后续工作。

#### 点问 6：指标汇总这一步具体汇总了什么？是Oracle的时序指标，还是你自己定义的业务可用性指标？带载压测时业务可用性怎么量化打分？
*考察维度：综合*

指标汇总由上游会话级 Oracle产生，不是我额外自定义的聚合。汇总的是Oracle的时序指标，包括故障发现时间、定位时间、修复动作时间、整体修复完成时间，以及框架本身的开销。带载压测时业务可用性由负载校验给出：它在带载状态下按预设压测脚本请求业务接口，看修复前后业务是否恢复可用。故障注入和负载回放同时启动，Oracle在带载状态下评估修复前后可用性，区别于空载评测。

**可能追问：**

- **追问：** 这些时序指标的口径是上游定义的还是你改过？
  口径是上游会话级 Oracle定义的，我没有改。我侧的工作是保证带载场景下负载正常起来、判分能采到修复前后对比，不重写判分逻辑。

- **追问：** 业务可用性怎么从压测结果量化成通过或不通过？
  由负载校验按预设压测脚本跑业务接口、对比修复前后是否恢复可用给出，是带载状态下的判定，不是我额外定义的评分函数。

#### 点问 7：你提到 LinUCB 重放在硬模式下正确率约 74%，这个奖励是真实Oracle给的吗？570 条用例怎么切的？
*考察维度：综合*

要说明清楚，重放阶段的奖励是脚本合成的代理标签，不是真实Oracle给的终态奖励。做法是针对每条故障按规则造出多个候选修复方案，正确修复打高分、错误值或错误资源类型打负分、干扰项打零分左右。数据集共 570 条用例，三个故障族各占约三分之一。硬模式是把所有非破坏性候选的风险标签统一压成低风险、并打乱候选顺序，避免排序器靠风险标签或顺序直接猜对，逼它从补丁路径、资源类型、与故障对齐程度等上下文特征里学。结果上：上下文老虎机正确选中比例约 74%，按最小风险取的基线约 34%，随机基线约 17%，不安全选择为零。真实多候选加真实Oracle奖励的在线版本尚未实现。

**可能追问：**

- **追问：** 奖励是合成的，74% 这个数字能说明什么？
  说明上下文老虎机在给定候选集上的排序能力上限，不是线上真实修复成功率。真实效果要等接回Oracle终态奖励后才能评估，当前是机制验证阶段。

- **追问：** 为什么用上下文老虎机而不是强化学习？
  上下文老虎机候选有限、奖励可打分，适合排序场景；且轻量、可解释、易做安全过滤。强化学习要状态转移和长期回报，当前阶段数据规模和闭环程度都不支撑。

### 相关八股储备（被问底层时怎么接）

- **上游编排器**
  - AIOpsLab 提供的故障注入与负载回放执行框架，负责注入故障、注册 agent、跑执行循环、结束复位、会话级 Oracle。
  - *最可能问：* 这条链路哪些是你自研的，哪些是复用上游？
  - → **简答：** 复用上游的是故障注入、负载回放、执行循环、会话判分这套基准框架；自研的是三类可观测数据接 Grafana 代理的工具、变更快照与ActionStack、轨迹解析与重放、老虎机排序和奖励构造这些和 SRE 决策直接相关的部分。
- **wrk2 负载回放**
  - 用脚本驱动压测工具按固定速率和连接数回放预设请求，在带载状态下配合Oracle评估可用性。
  - *最可能问：* 为什么评测要在带载状态下做？
  - → **简答：** 因为很多故障只有带真实流量才暴露得出业务影响——副本清零、端口改错这类，空载下集群看着没事，一压流量延迟飙升、报错率上升。带载评测用压测工具按固定速率回放请求、配合Oracle，才能量出真实的可用性变化，空载评测会漏判。
- **Kubernetes 资源 diff 与字段级校验**
  - 记录某资源指定字段修改前后的值，并核对修改后实际生效情况，如Endpoint是否对应到新端口、副本是否就绪、实例是否脱离挂起。
  - *最可能问：* 怎么确认 agent 改完后真的生效了，而不是改了没起作用？
  - → **简答：** 靠资源 diff 加字段级校验：记录目标字段改前改后的值，再核对改完后实际生效情况——端口类故障核对Endpoint是否对应到新端口、副本类核对是否就绪、挂起类核对实例是否脱离挂起。不是只看命令返回成功，而是读回集群确认值真的变了。
- **带校验的多阶段控制流**
  - 执行后先等校验等待时间再做集群健康校验，不通过则重试，最多重试若干次、每次间隔等待，用尽才判失败。
  - *最可能问：* 校验失败后是回滚重试还是直接判失败？
  - → **简答：** 先回滚再重试，重试到上限才判失败。执行后等一个校验等待时间再做集群健康校验，不通过就按后进先出回滚最近改动、进入下一轮重试，最多若干次、每次间隔等待，全部用尽才最终判失败。这样给系统多次机会收敛，不因一次抖动直接判败。
- **LinUCB 上下文老虎机**
  - 一种线性上下文老虎机算法，用上下文特征对每个臂估分排序来选最优臂，适合候选数有限、奖励可打分的排序场景。
  - *最可能问：* 为什么用上下文老虎机而不是强化学习或更复杂的模型？
  - → **简答：** 因为「给一个故障选一个缓解动作」是单步上下文决策——每次故障特征独立、只看即时奖励，没有跨步的状态转移和长程回报。强化学习要建模多步转移、样本需求大、训练不稳，对单步决策是杀鸡用牛刀；上下文老虎机简单稳定、样本效率高，正好匹配。
- **离线重放与代理奖励**
  - 用脚本合成的标签而非真实环境奖励做离线重放，验证排序算法在给定候选集上的能力，奖励与真实终态之间有差距。
  - *最可能问：* 离线重放的奖励能代表线上效果吗？
  - → **简答：** 只能近似、不能完全代表。重放用的是脚本合成的代理奖励，和真实终态奖励有差距，能验证排序算法在给定候选集上的相对能力，但绝对数值不能直接当线上效果。要更接近线上，得把基于真实终态的多分量奖励接进来、再上小流量在线验证。
- **沙箱受控执行与ActionStack**
  - 在受限namespace里真改并校验，每个改动动手前先存快照、按后进先出回滚，成功后也回滚以便复用沙箱。
  - *最可能问：* 沙箱执行和真实线上执行差在哪？
  - → **简答：** 主要差在隔离和复位。沙箱在受限namespace里真改真校验，但每次跑完会回滚清场、复用同一个干净环境，没有线上那种持续流量、并发变更和真实用户；线上是持续运行、变更累积、流量真实。所以沙箱验证的是「动作本身对不对」，线上还要额外扛住并发和持续负载。

### 上线 / 规模 / 后续（roadmap，自信带过，不是认怂）

这套体系分阶段推进。第一阶段做 live 链路：把故障注入、负载回放、agent 执行、环境复位、指标汇总跑通，并沉淀统一轨迹。第二阶段在沙箱里加带前置断言和ActionStack的受控执行，覆盖Service 端口、副本清零、调度到不存在节点三类配置类故障，补丁、改后校验、回滚三步全通过。第三阶段做离线重放：验证上下文老虎机在压掉候选风险泄露的硬模式下，正确选中比例约 74%，高于最小风险基线的 34% 和随机基线的 17%，且不安全选择为零。下一步是把离线合成的代理奖励替换成真实Oracle的终态奖励，把链路接回在线评测，扩大每类故障的样本规模，并引入性能类故障和更高并发的带载判定。现状是闭环验证集中在配置类故障、规模偏小，性能类故障和在线 Oracle闭环尚未覆盖。


---

## 简历条目 4｜Oracle 驱动 Tool-Calling 数据 + LoRA 微调 Qwen2.5-7B-Instruct Planner + LinUCB 候选排序 + 奖励构造

**简历原文：** 基于 Oracle 通过轨迹与失败纠错样本构造 Tool-Calling 数据，使用 LoRA 微调 Qwen2.5-7B-Instruct Mitigation Planner，优化修复计划生成、工具选择与参数填充；对 Planner 生成的安全候选计划使用 LinUCB 在沙箱 episode 中排序，以终态 Oracle、执行成本和回滚惩罚构造奖励。

### 面试官总问
> **Q：** 这条里你基于 Oracle 轨迹和失败纠错样本微调了 Qwen2.5-7B-Instruct 做 Mitigation Planner，再用 LinUCB 排序候选计划，具体怎么做的、效果怎么样？

**我的回答（开口讲）：**

这套工作分两个阶段，目标一是把开源大模型微调成能稳定输出结构化、合规的 K8s 修复计划，二是在生成出的候选计划上做一层排序选优。一是数据构造。我以 Oracle跑通的成功 episode 为锚，取这些 episode 里故障的namespace、workload、节点、端口等真实参数，填进修复计划的参数化模板合成训练对；参数取值来自真实 episode，计划正文是模板生成的，不是从 agent 运行日志里逐条抽取的。同时构造一组失败纠错样本，覆盖错值补丁、错资源类型、缺前置值断言、危险删除几类典型错误。二是模型微调。基座是 Qwen2.5-7B-Instruct，用 QLoRA，4 比特 NF4 量化加载，秩 16、alpha 32，覆盖注意力和多层感知机共七个线性层。监督微调通道跑的是纯文本计划正文，单故障族 198 条训练样本、五个 epoch，训练损失降到约 0.075、评估损失约 0.011，权重和损失曲线都有留存。三是候选排序。Planner 对一个故障产出多个安全候选计划，我用上下文老虎机做离线重放排序，奖励目前是按候选类型预先设定的固定分。在三个故障族、570 条 episode 的硬模式重放上，选对正确计划的比率约 73.7%，对照随机基线约 17%、只看风险最低约 33.6%，且未选中任何危险计划。终态、执行成本、回滚惩罚组成的多分量奖励已在另一条管线上实现，还没接进老虎机，是下一步。

### 点问展开（面试官点哪个名词都能接）

#### 点问 1：你说微调后评估损失收敛、还保存了权重，那最终损失具体到多少？训练曲线和 checkpoint 现在在哪？
*考察维度：综合*

训练用的是 Qwen2.5-7B-Instruct 加 QLoRA，单故障族 198 条训练、20 条评估，五个 epoch。训练损失最终降到约 0.075，评估损失约 0.011，没有发散。adapter 权重、两个中途 checkpoint、训练和评估的损失记录文件都留存了，训练曲线可以直接从损失记录文件还原。单次训练约 470 秒。这套损失值和权重产物都在，可以作为收敛的依据。下一步是把多故障族的数据扩充进来，样本量从约 198 条扩到约 460 条，再重训一版做对比。

**可能追问：**

- **追问：** 训练用的是 TRL 的 SFTTrainer 还是直接 transformers 的 Trainer？
  用的是 transformers 原生 Trainer，不是 TRL 的 SFTTrainer。我手写了数据集和数据整理逻辑，对 prompt 部分（系统提示和用户指令）把损失标签置为忽略值 -100，只对助手回答部分算损失，这样模型只学回答本身，不去背提示词。这套实现和 SFTTrainer 等价，但更底层、可控。

#### 点问 2：微调用的是什么基座模型、为什么选 QLoRA？
*考察维度：综合*

基座是 Qwen2.5-7B-Instruct。选 QLoRA 主要是显存和成本考虑。7B 全参微调显存需求大，QLoRA 把基座权重量化到 4 比特 NF4 加载，再在量化基座上训练一组低秩适配矩阵，显存显著下降，单卡能跑。配置上秩取 16、alpha 取 32，目标层覆盖注意力的四个投影加多层感知机的三个线性投影共七个线性层，优化器用 8 比特分页优化器，计算精度按显卡是否支持 bf16 自适应。这套配置既保留基座通用能力，又让适配矩阵专门学修复计划生成、工具选择和参数填充。

**可能追问：**

- **追问：** LoRA 的秩为什么取 16，再大会不会更好？
  秩 16 是这条任务的常用起点。修复计划结构规整、字段固定、操作类型有限，任务自由度不高，秩 16 已能覆盖。更大的秩在样本量只有两三百条时容易过拟合，且推理时适配矩阵更大、合并开销更高。我判断收敛是看训练损失和评估损失都稳定下降且不发散，目前这个秩在验证集上满足要求。

- **追问：** 训练样本不到两百条，会不会过拟合？
  是这条数据规模下的关注点。我控制过拟合靠三点：一是样本覆盖正反两类，包括正确计划和错值、错资源、缺前置等纠错样本；二是只训五个 epoch 并保留评估集监控损失；三是 LoRA 可训练参数占比很低。从训练损失约 0.075、评估损失约 0.011 都收敛且不发散看，过拟合可控。下一步是多故障族数据扩充到四百多条再验证。

#### 点问 3：训练数据是怎么从 Oracle 轨迹和失败纠错样本构造出来的？
*考察维度：场景项目实践*

数据以 Oracle跑通的成功 episode 为锚。成功 episode 记录了故障的namespace、workload、节点、端口等真实参数，我把这些参数填进修复计划的参数化模板，合成标准对话格式的训练对，内容是完整的修复计划文本。需要说明的是，参数取值来自真实 episode，计划正文是模板生成的，不是从 agent 运行日志里逐条抽取的。失败纠错样本按几种典型错误构造，包括补丁取错值、指错资源类型、缺前置值断言、危险删除。这些训练对都以验证通过的 episode 编号关联到真实跑通的 Oracle 成功用例。正样本教模型正确计划的结构，带负标签的纠错样本让模型在生成时更倾向规避这几类错误，规避能力主要还是靠后置的安全过滤兜底。

**可能追问：**

- **追问：** 失败纠错样本具体是哪几种错误？
  主要有几类：补丁里字段取值错误，比如端口写错；指错资源类型；缺少前置值断言，意味着没有先校验当前字段值就执行修改，并发改了会静默覆盖；还有危险删除资源的候选，这类会被安全策略直接拦截。覆盖这几类是让模型在生成阶段就倾向规避它们，不是单靠训练数据完全学会。

- **追问：** agent 运行日志里的轨迹你用在哪了？
  日志里的轨迹我主要用于打分和分析。它解析出来是事件流，格式不是干净的指令-回答对，agent 在一条 episode 里可能试错多次，哪一步算正样本需要人工判断，不适合直接抽监督样本。所以训练数据走的是以验证通过的 episode 参数为锚、模板合成计划正文这条路。把日志里的真实参数和动作序列作为模板输入，是后续可以加强的方向。

#### 点问 4：你Tool-Calling 格式的数据，到底有没有喂进这次微调？
*考察维度：编码能力*

这次微调实际喂进去的是纯文本计划正文通道，每个助手回答就是修复计划文本，模型直接学结构化输出。Tool-Calling 格式那套数据我造好了，格式是顶层带工具定义、助手回复以Tool-Calling 形式给出工具名和序列化参数，参数本身就是修复计划，但目前还没接进训练管线，不在这次 0.075 损失那次训练里。改善集中在输出的结构化和合规性上：未经微调的基座直接输出时常见格式漂移、字段缺失、参数取值不稳定，微调后更稳定地走标准对话加结构化输出，操作类型、资源、补丁、风险、前后置断言这些字段的填充实度和一致性明显提升。把Tool-Calling 数据并入训练、让它同时学会按Tool-Calling 规范填参数，是下一步。

**可能追问：**

- **追问：** Tool-Calling 的数据格式是怎样的？
  用标准Tool-Calling 格式。每条样本顶层带工具定义，描述修复计划工具的名称、功能和参数 schema；对话里助手那条消息以Tool-Calling 形式给出，内容包括工具名和序列化参数，参数就是修复计划的 JSON。这样模型学到的是先理解工具定义、再按调用规范填参数，而不是自由文本生成。

- **追问：** 怎么量化参数填充的改善？
  几个口径：一是格式校验通过率，即生成的计划能否被 schema 接受；二是字段齐全率，看必填字段是否缺失；三是参数与故障对齐率，比如端口补丁取值是否和故障链路片段一致。微调前基座在这几项上波动较大，微调后稳定收敛。

#### 点问 5：LinUCB 排序候选计划具体是怎么做的，奖励怎么定？
*考察维度：AI使用*

一个故障经 Planner 会产出多个安全候选计划，需要选一个最优执行。我把每个候选抽成一组上下文特征，从计划的字段派生，包括操作类型、资源类型是否匹配、namespace是否匹配、补丁路径、是否带前后置断言、是否带回滚快照、风险等级、故障对齐等，多故障重放用的是三十多维特征。奖励目前是按候选类型预先设定的固定分：正确补丁给正分，错值、错资源给负分，重启诱饵、合并诱饵居中，危险删除在排序前就被安全策略拦截。重放在三个故障族、570 条 episode 上跑，用多个种子和探索强度反复统计，最优探索强度下选对正确计划的比率约 73.7%。这里要区分清楚：老虎机重放用的是预设固定分，不是候选在沙箱里真实执行后算出来的。

**可能追问：**

- **追问：** 73.7% 用的奖励，是真实执行后算出来的，还是离线预先标的固定分？
  是离线预先按候选类型标的固定分，不是真实执行后的奖励。重放用的固定分按候选类型定，正确补丁正分、错值错资源负分、危险删除被拦截。终态、执行成本、回滚惩罚组成的多分量真实执行奖励已在另一条管线上实现，但那条管线目前只跑了少量 episode，还没接进老虎机，是下一步。

- **追问：** 候选计划是从哪来的，是同一个故障的不同变体吗？
  对，是针对同一个故障的标准修复做扰动生成的多个变体，包括正确的字段级补丁、缺前置断言的版本、错值版本、错资源类型版本、重启诱饵、合并补丁诱饵、危险删除。这些候选先过安全过滤，危险计划在排序前就被挡掉，排序只发生在安全候选之间。

- **追问：** 这是在线学习还是离线重放？
  离线重放，不是在线更新。我对固定的一批 episode 用多个种子和多个探索强度反复重放，统计选对率和平均奖励。接入沙箱真实Oracle奖励的在线更新是下一阶段的工作。

#### 点问 6：你训练只在单故障族上做了，但排序评估跨了三个故障族。训练时没见过的故障族，排序时是怎么覆盖的？
*考察维度：场景项目实践*

这里要把两个阶段分开看。微调阶段这次只在单故障族 198 条上跑出了 adapter，训练损失约 0.075、评估损失约 0.011。排序评估阶段是另一条独立的离线重放管线，覆盖三个故障族、570 条 episode；候选计划是按每个故障的参数用扰动模板生成的，上下文特征是从计划字段派生的，不依赖 Planner 是不是见过某个故障族。所以 73.7% 衡量的是排序算法在给定候选集合上的选优能力，不是这个微调后的 Planner 在三种故障上都训过。让 Planner 覆盖多故障族，需要把多故障 SFT 数据扩进来重训，是下一步。

**可能追问：**

- **追问：** 那排序和微调用的是同一个 Planner 吗？
  严格说不是同一份产物对齐的。排序重放里的候选是按参数化模板生成的安全候选，特征从计划字段派生，主要验证老虎机能不能在候选间选对，不强依赖某个特定版本的 Planner 输出。把微调后的 Planner 实际接入、由它产候选再交给老虎机排序，是下一步的端到端打通工作。

#### 点问 7：候选计划的安全过滤这一层是怎么实现的？
*考察维度：综合*

安全过滤在排序之前做。候选计划先生成，再过一层安全策略，主要按操作类型和风险等级判断：危险删除这类破坏性操作会被直接拦截，不计入排序。判定标准是候选里是否含删除、是否高风险，对齐到操作类型和风险字段。在 570 条 episode、每条七个候选的设置下，危险删除候选 570 条全部被拦截，剩下约 3420 条安全候选参与排序。会不会误杀正确计划这点：正确修复用的是字段级补丁，不是删除，结构上和安全策略不冲突，不会误杀。

**可能追问：**

- **追问：** 这个安全策略会不会误杀正确计划？
  不会。正确修复用的是字段级补丁，只改指定字段、不动其他字段，操作类型不是删除，和危险删除的拦截规则不重叠，所以安全过滤不会把正确候选挡掉。

#### 点问 8：73.7% 的选对率，这个数是怎么测出来的、可信吗？
*考察维度：场景项目实践*

这个数来自三个故障族、570 条 episode 的离线重放，每条 episode 有七个候选，总共约四千条候选，危险删除被安全策略拦截后剩三千多安全候选参与排序。为排除作弊，我用的是硬模式：把所有非破坏候选的风险都标成低，这样只看风险最低的基线没法靠风险标签区分，排序必须真正依赖补丁路径、资源类型、故障对齐、前后置断言这些计划特征。在这个设置下，老虎机选对比率约 73.7%，对照随机约 17%、只取最低风险约 33.6%，且选中危险计划比率为零，结果有按种子拆分的明细和汇总文件留存。需要说明的是，这是离线重放、奖励是预设固定分的结果，不能等同于真实集群的修复成功率。

**可能追问：**

- **追问：** 这个数和真实集群修复成功率之间是什么关系？
  目前是间接关系。73.7% 衡量的是在给定候选集合里老虎机能否把正确那个排到前面，用的是预设固定分。真实集群修复成功率取决于候选实际执行后终态Oracle是否通过，这需要把沙箱真实执行奖励接进来。所以这个数字证明的是排序算法在硬模式下显著优于基线，不是端到端修复成功率。

- **追问：** 随机基线只有 17%，是不是因为正确候选只有一个？
  对，每条 episode 里标为正确的候选只有一个，其余是扰动变体。七个候选里随机选一个，选对期望约七分之一，和测出来的 17% 一致。这也说明老虎机的 73.7% 是确实学到了区分特征，不是瞎蒙。

#### 点问 9：奖励里为什么要加执行成本和回滚惩罚，光看终态成功不够吗？
*考察维度：综合*

只看终态成功会鼓励一种倾向：只要最后修好，中间动多少手、回滚多少次都不计成本。但在真实集群里，每次实际写入都有扰动风险，回滚更说明前面的操作是错的。所以多分量奖励在终态基线上叠加成本侧扣分：实际执行一次和回滚一次各算一个执行成本单位按次扣分；执行了回滚额外扣一次；回滚失败扣得更重，因为系统既没修好又回不去，状态更糟。这样奖励偏好一次动手就修好、不需要回滚的计划，而不是反复试错最终蒙对的。前置断言不匹配、后置不通过、补丁执行失败也各有独立扣分，奖励信号既看结果也看过程。这套多分量奖励已在管线上实现，但还没接进老虎机。

**可能追问：**

- **追问：** 回滚失败的惩罚比回滚成功重，是怎么权衡的？
  回滚成功说明系统还能退回到动手前的快照，影响是额外执行成本；回滚失败说明系统既没修好又回不去，状态更糟，风险高一个量级，所以扣分明显更重。这反映回滚本身是兜底手段，兜底失败比兜底成功后果严重得多。

- **追问：** 这些惩罚系数怎么调？
  目前按经验给的一组默认值，终态通过给一个正的基线分，执行成本按单位线性扣，回滚惩罚逐级加重。调参依据是让奖励在数值上能区分一次修好、试错修好、回滚修好、回滚失败这几种情况，正负方向不互相抵消。下一步接真实执行奖励后，会根据实际分布再标定。

### 相关八股储备（被问底层时怎么接）

- **LoRA 与 QLoRA**
  - LoRA 在冻结的基座权重旁加一组低秩矩阵只训练这部分；QLoRA 进一步把基座量化到 4 比特 NF4 加载再训练 LoRA，显存大幅下降。
  - *最可能问：* 为什么用 QLoRA 不用全参微调？4 比特量化会不会影响精度？秩和 alpha 怎么选？
  - → **简答：** 用 QLoRA 是因为 7B 全参微调显存吃不消，QLoRA 把冻住的基座量化到 4 比特、只训练旁边低秩增量，显存降一个量级。精度几乎不掉：NF4 是按权重正态分布最优设计的量化格点、误差小，任务适配又集中在保持高精度的低秩增量上。秩用 16、缩放系数用 32，系数设成秩的两倍是经验默认，让增量有足够表达力又不喧宾夺主。
- **监督微调的损失掩码**
  - 在指令-回答对里只对助手回答部分算损失，系统提示和用户指令部分的标签置为 -100（忽略值），避免模型去背提示。
  - *最可能问：* 训练时 prompt 部分为什么不算损失？labels 置 -100 是什么意思？你是套 SFTTrainer 还是手写 Trainer？
  - → **简答：** 只对助手回答部分算损失、不背提示，是为了让模型学「怎么回答」而不是记住提问和系统提示。-100 是损失函数约定的忽略值，对应位置不参与交叉熵。我用的是基于 Transformers 的 Trainer 手写训练循环、用 PEFT 做 LoRA，手动做 -100 掩码，没用 TRL 的 SFTTrainer，这样掩码逻辑完全可控。
- **Tool-Calling 格式**
  - 标准格式是顶层声明工具定义，助手回复以结构化Tool-Calling 形式给出工具名和序列化参数，模型学习按调用规范填参数而非自由生成。
  - *最可能问：* Tool-Calling 数据和普通监督微调有什么不同？怎么让模型学会调工具？toolcall 数据训了没有？
  - → **简答：** Tool-Calling 数据多了「工具定义」和「按规范填的工具调用」两部分，模型学的是在给定工具集下按结构化格式输出工具名和参数。让它学会靠把这些样本喂进监督微调。现状是这部分数据已造好但还没接进微调管线，目前微调走的是结构化规划这条通道，Tool-Calling 通道是下一步。
- **LinUCB 上下文老虎机**
  - 维护每个臂的特征矩阵和奖励向量，用线性回归估计每个臂的期望回报，再加一个随不确定性递减的探索项，在利用和探索间平衡。
  - *最可能问：* LinUCB 和普通多臂老虎机区别在哪？探索项里的 alpha 是干嘛的？在线和离线重放的区别？
  - → **简答：** 普通多臂老虎机没有上下文、每个臂只有一个固定期望；LinUCB 每次带一个上下文特征，假设每个臂的期望收益是上下文的线性函数，能按不同故障特征给不同推荐。alpha 是探索强度旋钮，控制利用和试新之间的平衡，我网格搜最优是 1.0。在线是边选边拿真实奖励更新、要真交互；离线重放在固定历史数据上反复跑、不真交互，用于上线前筛策略定超参。
- **离线策略评估**
  - 在固定的历史数据集上反复重放，统计某策略的指标，不与环境真实交互，用于在上线前验证策略好坏。
  - *最可能问：* 离线重放的结论能直接代表上线效果吗？离线和在线学习的根本区别是什么？
  - → **简答：** 不能完全代表。离线奖励基于历史数据估，能否反映线上取决于数据覆盖度和分布是否一致，策略若总选历史没试过的臂就估不准。根本区别是离线没有真实反馈回路、只在固定数据上算，在线是选了动作、拿真实奖励、再更新的闭环。
- **奖励设计**
  - 把多个目标合成一个标量奖励，终态结果给主信号，执行成本和回滚等过程项给扣分，引导策略偏好一次成功的计划。
  - *最可能问：* 为什么不光看终态成功？回滚惩罚怎么定权重？重放用的奖励是真实执行还是预设固定分？
  - → **简答：** 不光看终态，是因为一次成功但绕远、反复回滚的计划工程上不如一次到位，所以要加执行成本和回滚惩罚引导策略偏好一次成功的干净计划。回滚惩罚权重靠经验设定、和多分量奖励一起调。现状是老虎机重放用的是按候选类型预设的固定分，基于真实终态的多分量奖励已在另一条管线上实现、还没接进排序，这是下一步。
- **字段级补丁与前置值断言**
  - 字段级补丁只改指定字段不动其他字段；前置值断言是先校验当前字段值对得上再改，并发改了就不动手，相当于乐观锁。
  - *最可能问：* 为什么不整个覆盖？值断言和 K8s 的 resourceVersion 乐观锁是什么关系？
  - → **简答：** 不整覆盖是为了最小变更，只改需要动的字段、别的字段一概不碰，避免把别人并发改的字段意外冲掉。值断言是动手前先校验当前字段值对得上预期再改，本质上就是乐观锁，和 K8s 的 resourceVersion 同理：先读一个版本、改时确认没被别人动过，对得上才改、对不上就放弃。

### 上线 / 规模 / 后续（roadmap，自信带过，不是认怂）

这套工作按阶段推进。第一阶段完成了数据构造和单故障族的 QLoRA 微调，产出了 adapter 权重，训练损失约 0.075、评估损失约 0.011，损失曲线和权重都有留存，监督微调通道跑通。第二阶段把候选排序接进来，用上下文老虎机在安全候选上做离线重放，在三个故障族、570 条 episode 的硬模式重放上选对率约 73.7%，显著优于基线且未选危险计划。现状有两点局限：一是老虎机重放用的奖励是按候选类型预设的固定分，终态、执行成本、回滚惩罚组成的多分量奖励已在另一条管线上实现，但那条管线目前只跑了少量 episode，还没接进老虎机；二是Tool-Calling 格式的训练数据已经造好，但还没接进监督微调管线。下一步先把多分量真实执行奖励接入排序管线，实现基于真实终态的奖励更新，再把Tool-Calling 数据并入训练，并扩充多故障族样本量，让 Planner 在多故障族上重训一版。


---

## 专题｜Jaeger 与分布式链路追踪

**简历原文：** (专题) Jaeger 与分布式链路追踪：它是什么、为什么 SRE/Agent 要用、被问到怎么答

### 面试官总问
> **Q：** 你简历里写了 Jaeger 和分布式链路追踪这块，能讲讲它是什么、你在这个系统里怎么用的、为什么 SRE 和 Agent 要用它？

**我的回答（开口讲）：**

分布式链路追踪解决的是一个问题：一个请求穿过几十个微服务后，慢或错具体发生在哪一跳。Jaeger 是 CNCF 毕业的分布式追踪后端，由 Uber 在 2015 年创建、2019 年毕业，负责把采集到的调用链存下来、提供查询和界面；采集标准由 OpenTelemetry 提供，两者分工明确，OpenTelemetry 管采集，Jaeger 管存储和查询。一条调用链由若干个调用片段按父子关系组成一棵树，靠同一个链路标识贯穿所有服务，链路标识随请求头跨进程透传。\n\n为什么 SRE 和 Agent 要用，有三点。第一，链路能把一个请求在所有服务上的耗时和错误串成一条因果链，直接看到某一跳占了多少毫秒、哪一跳报错，指标只显示延迟升高但无法定位是哪个服务导致的，日志分散在各服务、没有跨服务因果，这两者单独做不到。第二，链路适合跨服务根因定位，上层服务的错误通常是因为它所调用的下游返回了错误，沿父子关系回溯能定位到最深层那一跳。第三，在这个项目里我用它做了一条数据通路，把自然语言查链路、大模型摘要、根因回溯接起来，下面详细讲。

### 点问展开（面试官点哪个名词都能接）

#### 点问 1：Jaeger 到底是什么，它和 OpenTelemetry 是什么关系？
*考察维度：综合*

Jaeger 是一个开源的分布式追踪平台，由 Uber 在 2015 年创建，2016 年开源后捐给 CNCF，2019 年 10 月从 CNCF 毕业。CNCF 的成熟度分三档，从沙盒、孵化到毕业，毕业是最高成熟度等级，Jaeger 和 OpenTelemetry 同属 CNCF 的追踪生态。两者的分工是：OpenTelemetry 是采集标准，提供接口、SDK 和采集器，定义数据怎么采、怎么传；Jaeger 是后端，负责把采集到的调用链存下来、提供查询和可视化界面。Jaeger 灵感来自 Google 2010 年的 Dapper 论文。Jaeger 在 2024 年 11 月发了 v2，把自身构建在 OpenTelemetry 采集器框架之上，原生支持采集协议，消除了中间的协议翻译层。所以不要把 Jaeger 说成采集器，它是存储加查询的后端。

**可能追问：**

- **追问：** Jaeger 数据存在哪？怎么收费？
  主要支持三种分布式存储后端：Cassandra、Elasticsearch、OpenSearch。官方更推荐 Elasticsearch 或 OpenSearch，因为 Cassandra 是键值存储，对 Jaeger 的查询模式不友好。另有内存存储，只用于测试。Jaeger 本身开源免费，成本在存储集群和采集、采样开销上；托管服务版按摄入量和存储量计费。本项目自建，用内存存储做实验。

#### 点问 2：什么是 trace 和 span，它们是什么结构？span 和 span context 有什么区别？
*考察维度：综合*

一条调用链由若干个调用片段通过父子关系组成，代表一个请求在整个系统里的完整路径，结构上是一棵树，有一个根片段，下面可以有多个并行的子片段。每个调用片段是一个逻辑工作单元，至少包含操作名、起始时间、持续时长。每个片段还带两个核心字段：自己的片段标识和父片段标识，父片段标识指向父节点的片段标识，根片段的父标识为空。这是 span 本身的工作记录，还包含键值对标注和带时间戳的事件两类数据。需要区分的是 span 和 span context：span 是完整的工作记录，含持续时长、标注、事件；span context 是其中可注入到请求头跨进程传播的那一小段标识，由链路标识、片段标识、采样标志组成。在项目里我按链路标识聚合同一条链的所有片段，再靠父标识一路向上回溯找根。

**可能追问：**

- **追问：** 怎么标记一个片段是错误？
  我同时认四种错误标记：HTTP 状态码等于五百、错误标记为真、采集标准状态为错误、gRPC 状态码等于十四（gRPC 的 UNKNOWN）。因为 OpenTelemetry 埋点和不同协议打的错误标记不一致，只认一种会漏判，任意一种命中就认为这一片段有错误。

- **追问：** 一条链路怎么唯一标识？trace-id 和 span-id 各多长？
  靠链路标识，全局唯一。链路标识是十六字节、写成三十二位十六进制串；片段标识是八字节、写成十六位十六进制。同一个请求在所有服务上产生的所有片段都带同一个链路标识，所以能把它们聚合成一条完整的链。

#### 点问 3：不同服务的调用片段怎么串成一条链？靠什么跨进程透传？
*考察维度：综合*

靠请求头跨进程透传 span context。OpenTelemetry 默认用 W3C 的链路上下文规范，对应一个请求头字段，格式是四段：版本号、链路标识、父标识、采样标志。我背一个完整样例：版本号零零、链路标识三十二位十六进制、父标识十六位十六进制、采样标志零一表示采样。链路是这样串起来的：入口服务生成链路标识，每次出站调用把当前片段的 span context 注入请求头，下游服务收到后把父标识作为自己片段的父，于是同一个链路标识贯穿所有服务。透传的是 span context 这一小段标识，不是 span 本身。如果透传失败，下游拿不到父上下文，只能另起一个链路标识，原本一条请求会被拆成几段互不关联的链，定位根因会失败，这是生产里链路只有半截的最常见原因。

**可能追问：**

- **追问：** 请求头字段的具体格式能说一下吗？
  四段用横线分隔：版本号两位、链路标识三十二位十六进制、父标识十六位十六进制、采样标志两位十六进制。采样标志零一表示这条链被采样、零零表示不采。

- **追问：** 服务网格层会自动透传吗？
  网格层会自动注入和转发这些请求头，但存在头部没被正确透传导致链路断裂的已知问题。所以即使有网格层，应用层埋点也要正确处理这些头。

#### 点问 4：为什么指标加日志还不够，非要加链路追踪？
*考察维度：综合*

可观测性有三个支柱：指标回答多少，比如吞吐量、延迟；日志回答发生了什么，是离散事件；链路回答在哪一跳，是请求级的因果链。指标的问题在于它只说平均延迟或某个分位升高了，但无法定位是哪个服务导致的；日志分散在每个服务里，没有跨服务的因果关联。链路用同一个链路标识把一个请求在所有服务上的所有片段串成一条因果链，能直接看到总耗时里某个服务占了多少毫秒、其中调用另一个服务又占了多少，精确定位瓶颈和错误在哪一跳。三者互补，没有绝对优先级：指标适合告警和大盘、便宜且聚合；日志适合事后深挖细节；链路适合跨服务根因定位，但比较贵，需要采样。

**可能追问：**

- **追问：** OpenTelemetry 三支柱都覆盖吗？
  是，OpenTelemetry 现在统一了三种信号的采集标准，指标、日志、链路都覆盖。但 Jaeger 只负责链路这一种信号的后端，不管指标和日志。

- **追问：** 三者怎么排优先级？
  没有绝对顺序。指标适合告警和大盘监控，便宜、聚合；日志适合事后深挖细节；链路适合跨服务根因定位，贵、需要采样。实际按场景组合用。

#### 点问 5：采样是什么，为什么要采样？head-based 和 tail-based 有什么区别？下游怎么保证采样一致？
*考察维度：综合*

采样是因为生产环境请求量巨大，全量采集所有片段的存储和网络成本不可接受，所以只采一部分链路。分两种：一是头部采样，在链路一进入就决定采不采，开销小，而且采样决定会随 span context 一起透传，下游服务看到采样标志就一致执行，不会出现一条链一半采一半不采；但它是盲采，会随机漏掉错误和慢请求。二是尾部采样，在采集器上缓冲完一整条链再按规则决定，比如有错误必采、延迟超阈值必采，能保证坏链不丢，但要在采集器上把所有片段先攒住，内存和延迟开销大。工程上通常先用头部砍掉一部分，再用尾部精筛，两者配合。之所以不全量采，是量太大、存储贵，而且绝大多数正常链路没有诊断价值。

**可能追问：**

- **追问：** 头部采样为什么能保证整条链采样一致？
  因为采样标志随 span context 一起透传。入口服务在根片段上决定采不采，把这个决定写进采样标志，随请求头传到下游，下游看到采样标志就一致执行，不会出现一条链一半采一半不采。

- **追问：** 尾部采样在哪做？为什么不能百分之百全采？
  在采集器上做，先把一整条链的所有片段缓冲住再按规则决定。量太大、存储成本高，而且绝大多数链路是正常的没有诊断价值，全采既贵又淹没真正有问题的链路，所以用采样聚焦在错误和慢请求上。

#### 点问 6：你在项目里具体怎么用 Jaeger 的？打的是哪个接口、URL 是什么？Jaeger 和 Grafana 是什么关系？
*考察维度：场景项目实践*

这条数据通路是这样跑的：测试用的是 DeathStarBench 的社交网络应用，一组用微服务架构搭建的社交网络服务，被注入故障后业务请求链路里某一片段变慢或报错。我有一个自然语言到链路查询的转换模块，把哪个服务、哪一跳、哪个链路标识翻译成对 Jaeger 查询接口的调用，再交给大模型做摘要和根因推断。具体打的是哪个接口：查询经 Grafana 作为数据源代理暴露在 /jaeger/api/traces Endpoint下，按服务名、操作名、时间窗、错误标记过滤，Grafana 在这里作为统一入口把 Jaeger 的查询接口代理出来。错误标记我同时认四种：HTTP 状态码等于五百、错误标记为真、采集标准状态为错误、gRPC 状态码等于十四，因为不同协议、不同埋点方式打的错误标记不一致，任意一种命中就判为错误。转换模块之上还加了一层校验，校验服务名、操作名、时间窗不超过十二小时、数量上限一到十，防止大模型生成无效查询。

**可能追问：**

- **追问：** Grafana 和 Jaeger 在你的系统里是什么关系？
  Grafana 作为数据源代理和统一入口，把 Jaeger 的查询接口代理在 /jaeger/api 下。Agent 不直接连 Jaeger，而是经 Grafana 转发查询，这样链路查询和其他数据源可以在同一入口统一管理。

- **追问：** 为什么错误判定要认四种标记？
  因为 OpenTelemetry 埋点和不同协议、不同框架打的错误标记不一样。HTTP 走状态码，采集标准走自己的状态字段，gRPC 走自己的状态码。只认一种会漏判，所以我同时认四种，任意一种命中就认为这一片段有错误。

#### 点问 7：你根因定位找的是最深层报错的叶子片段，那你怎么保证它就是真根因、会不会误判？
*考察维度：场景项目实践*

在调用树里，上层服务的报错通常是因为它所调用的下游返回了错误，所以第一个报错的片段往往是调用方而不是根因；我沿父标识回溯到最深层、有错误且没有子片段的叶子节点，作为根因的候选线索，输出给诊断 Agent 再结合其他信号确认。需要说明的是，这个叶子节点不一定是最终根因，它只是分布式系统里错误最深层的位置。下一步规划是把根因回溯和指标、日志做交叉验证，不止定位到最深层报错叶子，还能结合多个信号判断它是不是真正根因，减少误判。这是分阶段推进的：第一步打通数据通路能定位到具体服务，第二步加多协议错误识别和查询校验，下一步做跨信号交叉验证。

**可能追问：**

- **追问：** 根因为什么不是第一个报错的片段？
  在调用树里，上层服务的报错通常是因为它所调用的下游返回了错误，所以第一个报错的片段往往是调用方而不是根因。我沿父标识回溯到最深层、有错误且没有子片段的叶子节点作为根因候选线索。

- **追问：** 怎么判断回溯时会不会遇到环？
  回溯时记录已访问的片段标识，如果再次遇到已访问的节点说明存在环，会中断这条回溯路径，避免死循环。这是分布式系统里偶发的埋点异常，校验逻辑会处理。

#### 点问 8：你这个测试用的是哪个微服务应用？叫什么名字、大概几个服务、什么架构？
*考察维度：场景项目实践*

测试用的是 DeathStarBench 的社交网络应用，是一套开源的微服务基准测试应用，模拟社交网络的核心功能，比如用户注册、发帖、时间线、关系图谱。它用微服务架构搭建，由若干个相互调用的服务组成，覆盖 HTTP 和 gRPC 两种调用协议，这正是我的错误判定要同时认四种错误标记的原因。在这个应用上注入 CPU、内存、网络等故障，业务请求链路里对应片段变慢或报错，链路追踪把故障定位到具体服务和操作。选这个应用是因为它服务划分清晰、调用链有多跳，适合验证跨服务根因定位。

**可能追问：**

- **追问：** 为什么选 DeathStarBench 而不是别的？
  它是学术界和工业界常用的微服务基准，服务划分清晰、调用链有多跳，覆盖 HTTP 和 gRPC 两种协议，适合验证跨服务根因定位和多协议错误标记识别。

### 相关八股储备（被问底层时怎么接）

- **分布式链路追踪基本概念**
  - 把一个请求在多个微服务上的调用过程串成一条因果链，精确定位慢或错发生在哪一跳。
  - *最可能问：* 什么是分布式追踪，为什么微服务架构下需要它？
  - → **简答：** 分布式追踪把一个请求在多个微服务上的调用过程串成一条因果链，标出每一跳的耗时和错误。微服务架构下一个请求跨多个服务，出问题时光看单服务日志看不出卡在哪一跳，追踪能精确定位慢或错发生在哪一跳。
- **trace 与 span 的关系、span 与 span context 的区别**
  - 一条链路由若干调用片段按父子关系组成一棵树，片段含操作名、起止时间、持续时长、错误标记、自己的片段标识和父片段标识；span context 是其中可跨进程传播的标识段，含链路标识、片段标识、采样标志。
  - *最可能问：* trace 是树还是链表？span 之间靠什么建立父子关系？span 和 span context 有什么区别？
  - → **简答：** trace 是一棵树（一个请求可有多次并发下游调用，所以是树不是链表），片段之间靠父子标识建立关系：每个片段带自己的标识和父片段标识。片段是一次调用的完整记录（含操作名、耗时、错误等），片段上下文是其中可跨进程传播的那部分标识（链路标识、片段标识、采样标志），用于把片段串起来。
- **上下文透传**
  - span context 靠请求头字段跨进程传递，下游用父标识作为自己片段的父，把同一请求在所有服务的片段串成一条链。
  - *最可能问：* 微服务怎么串成一条链？透传的是 span 还是 span context？透传失败会怎样？
  - → **简答：** 透传的是片段上下文（不是整个片段），靠请求头跨进程传，下游拿到父标识作为自己片段的父，把同一请求在所有服务的片段串成一条链。透传失败链路就断成几段，常见原因是某服务没自动注入头、或用了非 HTTP 协议没透传。
- **采样及采样一致性**
  - 因请求量巨大只采一部分链路，分头部和尾部两种；头部采样的决定随 span context 透传，下游看到采样标志就一致执行。
  - *最可能问：* 为什么要采样？头部和尾部采样怎么选？头部采样怎么保证整条链一致？
  - → **简答：** 采样是因为请求量巨大、全采存储和开销吃不消，只采一部分链路。头部采样在入口就决定采不采、决定随片段上下文透传，下游看到采样标志就一致执行，保证整条链要么全采要么全不采；尾部采样先全采、看完结果（比如只留出错的）再决定留不留，能精准抓异常链但开销大。
- **可观测性三支柱**
  - 指标、日志、链路三者互补，分别回答多少、发生了什么、在哪一跳。
  - *最可能问：* 指标、日志、链路三选一怎么排优先级？
  - → **简答：** 不是三选一而是互补：指标回答「多少」（聚合数字，做监控告警）、日志回答「发生了什么」（单点细节）、链路回答「在哪一跳」（跨服务定位）。优先级看场景：日常监控告警指标优先；排障时先看指标定方向、再用链路定位到具体服务、最后用日志看细节。
- **Jaeger 与 OpenTelemetry 分工**
  - OpenTelemetry 是采集标准和采集器，Jaeger 是存储加查询后端，两者同属 CNCF 追踪生态。
  - *最可能问：* Jaeger 和 OpenTelemetry 是什么关系？Jaeger 存数据存哪？
  - → **简答：** OpenTelemetry 是采集标准加采集器（定义数据格式、负责收集导出），Jaeger 是存储加查询后端（存链路、提供查询界面），两者同属 CNCF 追踪生态、配合使用。Jaeger 数据通常存到 Elasticsearch 或 Cassandra 这类后端做持久化。
- **traceparent 格式与标识长度**
  - W3C 链路上下文头由四段组成：版本号两位、链路标识三十二位十六进制（十六字节）、父标识十六位十六进制（八字节）、采样标志两位。
  - *最可能问：* traceparent 格式是什么？trace-id 和 span-id 各多少字节？
  - → **简答：** traceparent 是 W3C 链路上下文头，四段：版本号（两位十六进制）、链路标识（三十二位十六进制、十六字节）、父标识（十六位十六进制、八字节）、采样标志（两位）。所以链路标识是 16 字节、父标识是 8 字节。
- **大模型工具调用与自然语言转查询**
  - 用大模型把自然语言意图转成结构化查询参数，查询经 Grafana 代理走 /jaeger/api/traces，再用校验层把关参数合法性防止无效查询。
  - *最可能问：* 怎么让 Agent 自己查链路？大模型生成的查询怎么保证有效？具体打哪个接口？
  - → **简答：** 让大模型把自然语言意图转成结构化查询参数，查询经 Grafana 代理打到链路后端的查询接口，再用一层校验把关参数合法性——只放行合法字段和格式、挡掉无效查询。模型负责理解意图、校验层负责兜底，避免模型瞎拼查询。
- **根因定位的回溯逻辑**
  - 分布式系统里上层服务错误通常因下游出错，沿父标识回溯找最深层报错的叶子片段作为根因候选线索，不一定是最终根因，需结合其他信号交叉验证。
  - *最可能问：* 为什么不取第一个报错的片段作为根因？叶子片段一定是根因吗？
  - → **简答：** 不取第一个报错的，因为分布式系统里上层服务的错误通常是下游出错传导上来的，第一个报错的往往是受害者不是根因。沿父子关系回溯到最深层报错的叶子片段作为根因候选线索，但叶子不一定是最终根因——可能是更深的依赖没暴露、或外部因素，需要结合指标日志交叉验证。

### 上线 / 规模 / 后续（roadmap，自信带过，不是认怂）

现状是分阶段推进的。第一阶段打通了数据通路：自然语言转查询、查 /jaeger/api/traces 接口、大模型摘要这条链已经跑通，能正确把注入故障定位到具体服务和操作，测试应用是 DeathStarBench 的社交网络应用。第二阶段加上了错误标记的多协议兼容和查询校验层，保证不同埋点方式都能识别错误、大模型不会生成无效查询，时间窗校验在十二小时内、数量上限一到十。目前落地的是在这套基准微服务上验证，存储用的是内存或自建存储做实验，规模和真实生产还有差距，根因回溯输出的是候选线索而不是确认根因。下一步一是把头部采样和尾部采样配合接进来，降低高流量场景下的开销；二是在根因回溯上结合指标和日志做交叉验证，不止定位到最深层报错叶子，还能判断它是不是真正根因；三是把存储切到正式的分布式存储后端，做更大规模的压力验证。


---

## 专题｜实时数据流怎么处理

### 面试官总问

> **Q：** 你这套系统的实时数据流是怎么处理的？数据是流式进来还是你主动拉？

**我的回答（开口讲）：**

这套系统的数据获取是按轮次主动拉快照的请求-响应模型，不是 Kafka、Flink 那种持续订阅的流式管道。整个 agent 协同一轮一轮跑，每一轮里各角色用到数据时就调一次工具、拉一份当前时刻的快照，做完一轮修复进入下一轮时再重新拉，数据是每轮新鲜的。

具体分三个要点。一是数据源：在带可观测栈的本地集群底座上，分布式 Trace、日志、指标三类数据分别接 Jaeger、Loki、Prometheus，统一经 Grafana 数据源代理访问，集群资源状态用 kubectl 命令读；每次拉取都以当前时刻为窗口右端、向前回看一段时间，Trace 默认回看最近十分钟。二是时序：不存在后台轮询定时器、不存在 webhook 推送，每次取数都由这一轮的工具调用显式触发，取回的是一次性快照，agent 看到的就是调用那一刻的最新状态。三是两种底座差异：带可观测栈的底座走真实的可观测后端、经 Grafana 代理；基准评测底座走基准自带的生成器工具，由编排器同步执行一次拉取再返回，两条路都是同步快照、没有订阅。

这样设计的考虑是：SRE 故障处理是回合制决策，每一轮需要的是此刻集群与业务的完整切片来支撑定位和修复判断，而不是高频事件流；回合制拉快照实现简单、每次决策基于一致的全量快照、可复盘，足够覆盖当前的故障定位和修复评估。下一步若要接秒级实时检测，会另起一条流式采集加滑动窗口告警的在线链路，那是独立的扩展。

### 点问展开（面试官点哪个都能接）

#### 点问 1：为什么不用 Kafka/流式，而用拉快照？
这套系统的决策是回合制的：每一轮要基于此刻完整的集群状态和可观测数据做判断，不是对一个个实时事件做增量反应。回合制拉快照有三点好处：实现简单、每次决策基于一致的全量切片不会事件乱序、整条轨迹可复盘。流式管道适合秒级异常检测这种高频场景，当前阶段先把定位-修复-回滚的回合制闭环跑通，实时检测是后续在线链路的事。

- **追问：拉快照会不会漏掉转瞬即逝的故障？**
  会漏掉纯瞬时、过后不留痕迹的事件。但 SRE 关心的多数故障（配置写错、副本清零、端口错配）在集群里会持续存在，拉快照时仍然可见，不依赖实时捕捉，回看最近一段时间的快照就能定位。
- **追问：每次拉最近十分钟，数据量会不会很大？**
  Trace 和日志会带时间窗和服务过滤，只拉涉及嫌疑服务的部分，不是全量；指标也是按需查特定序列。窗口大小可配，评测底座流量小、十分钟足够。

#### 点问 2：数据新鲜度怎么保证？有没有延迟问题？
新鲜度靠「每次工具调用都以当前时刻为窗口右端」保证——agent 这一轮看到的就是调用那一刻往前回看的数据，不存在缓存陈旧。延迟方面，可观测后端本身有采集和索引延迟（秒级），但 SRE 故障处理是分钟级回合制决策，这个延迟不影响判断。

- **追问：如果故障发生在回看窗口之前呢？**
  窗口可以调大，定位不到时扩大回看范围；而且故障只要还在持续，当前窗口就能看到它的表现。
- **追问：两轮之间的状态变化你怎么感知？**
  靠每轮重新拉快照做对比：这一轮拉到的状态和上一轮的轨迹记录对比，就能看出修复动作是否生效、字段是否变到目标值。

#### 点问 3：这种模型能不能做秒级实时检测？
当前这套不能，它是分钟级回合制决策，不是实时检测引擎。要做秒级检测需要另起一条在线链路：用流式采集（指标走远程写入、Trace 走采集器）持续进系统，配合滑动窗口和阈值告警，检测到异常再触发回合制的定位-修复流程。也就是实时检测做触发、回合制 agent 做处理，两层配合。

- **追问：那你现在这套更像什么？**
  更像一个按需拉取的回合制诊断与执行系统，数据获取是请求-响应式，和持续监控的流式系统是两种形态。
- **追问：流式接进来要改哪些？**
  主要是加一个持续采集和事件检测的前置层（流式接入加滑动窗口告警），它的输出作为触发信号喂给现有回合制 agent；agent 每轮拉快照的逻辑可以保留，也可以在流式层维护一个实时状态缓存供 agent 读。

#### 点问 4：两种底座的数据通路有什么不同？
带可观测栈的本地集群底座，三类数据分别接 Jaeger、Loki、Prometheus，统一经 Grafana 数据源代理访问、带鉴权，是贴近生产的真实可观测栈。基准评测底座走基准自带的生成器工具，agent 发一条命令、编排器同步执行一次拉取（Trace 经端口转发直连 Jaeger、日志经 K8s API 读 Pod 日志），拉完落成文件再给 agent 读；这条路的可观测后端是基准配套搭好的、不经 Grafana。两条路都是同步快照模型，差异只在数据怎么到 agent。

- **追问：为什么基准底座不经 Grafana？**
  基准底座是评测用的、自带一套可观测设施，直接用它的接口更简单可控；Grafana 代理那套是给本地实验集群做的统一入口。两边数据语义一致，通路不同。
- **追问：基准底座的指标为什么说暂时没有现成工具？**
  基准自带的指标采集在这套工具里覆盖不全，指标类数据在基准底座上暂时缺现成工具，这是已知的覆盖差异；带可观测栈底座上三类数据齐全。

### 相关八股储备（被问底层时怎么接）

- **拉取式 vs 推送式/流式数据获取**
  - 拉取式是消费方需要时主动发请求取一份当前数据；推送式/流式是数据源持续把数据推给消费方。本系统是拉取式：每轮按需拉快照。
  - *最可能问：* 拉取和流式各适合什么场景？为什么 SRE 回合制决策选拉取？
  - → **简答：** 拉取适合回合制、需要一致全量快照的决策；流式适合高频实时检测和低延迟告警。SRE 故障处理是分钟级回合制，每一轮要基于此刻完整切片判断，拉快照实现简单、状态一致、可复盘，所以选拉取。
- **回合制 agent 与流式 agent**
  - 回合制是 agent 一轮一轮处理、每轮拉一次状态；流式是 agent 持续消费事件流、实时反应。本系统是回合制。
  - *最可能问：* 回合制和流式 agent 各有什么优劣？你的系统为什么是回合制？
  - → **简答：** 回合制简单、可复盘、每轮决策基于一致快照，但响应是分钟级不是秒级；流式低延迟、能实时反应，但状态管理复杂、事件乱序难处理。这套系统做的是定位-修复-回滚这种重决策，回合制匹配；实时检测是另一层，后续接。

### 上线 / 规模 / 后续（roadmap）

现状是数据获取走回合制拉快照，带可观测栈底座三类数据齐全、基准底座缺指标类现成工具，这是已知覆盖差异。这套足够支撑当前的故障定位和修复评估。下一步两个方向：一是补齐基准底座的指标工具，让两种底座数据覆盖一致；二是若要做秒级实时检测，另起一条流式采集加滑动窗口告警的在线链路，检测到异常再触发现有回合制 agent，两层配合。

---

## 附录｜技术核查 backbone（仅供你自己心里有底，面试官看不到）

> 下面是从你真实代码里核查出来的事实（带 file:line 级证据），是你「自信讲」的地基。面试时不用背这些，但要知道每个名词背后真实机制是什么。

### 核查：architecture
*这是一个基于 ITBench/STRATUS 改造、用 CrewAI 编排的 SRE 系统，跑在 AIOpsLab（基准）或 ITBench/Kind（带 Grafana 可观测栈）上。核心结论：(1) "权限隔离"不是 A-Lock 读写锁，而是按 CrewAI role 把不同 tool 子集分给 diagnosis/mitigation/rollback 三个 agent —— 代码里完全没有 A-Lock / rwlock 这种东西，grep 零命中。(2) 确定性控制流 = CrewAI Process.sequential + 固定 task 列表：mitigation 模式顺序是 Rollback → initial_analysis → Mitigation；没有独立的 "Detection" agent，"detection" 只是 AIOpsLab 下的一个 task_type 标志。(3) Undo/Rollback 是独立的第一步 task（sre_rollback_task），用基于 ActionStack 的快照回滚，不是最后一步。(4) 实际只有 3 个真正 agent（diagnosis/mitigation/rollback），不是简历里说的 4 个 Detection/Diagnosis/Mitigation/Undo。(5) NL2Kubectl 通过 subprocess+shell=True 调 kubectl 二进制（不是 client-python），kubernetes client-python 只在 KubeCtl.__init__ 里 load_kube_config() 加载过 kubeconfig，API 调用全注释掉了。(6) 三类可观测数据：NL2Traces 接 Jaeger、NL2Metrics 接 Prometheus、NL2Logs 接 Loki，统一经 Grafana datasource proxy。这些 NL2* 工具只在 ITBench 模式启用，AIOpsLab 模式走 GetTracesTool/ReadTracesTool/GetLogsTool 这套（从 AIOpsLab generator 拿数据，不经 Grafana）。需要注意：NL2Kubectl 在 diagnosis 和 mitigation 两个 task 里都被装配（diagnosis 也能写集群），所以"诊断只读、缓解才写"是配置层的软隔离，不是硬权限。*

- ✅ **权限/读写隔离是靠按 CrewAI role 分配不同 tool 子集实现的，并不是一个真正的 A-Lock / 读写锁。diagnosis agent 拿读类+NL2Kubectl，mitigation agent 多了 MitigationCustomTool/WaitTool，rollback agent 只拿 RollbackTool。**
  - 证据：crew.py:112-258 每个 @agent 的 tools=[] 都是空的，工具是在 @task 层装配的；crew.py:172-229 ITBench 模式下 diagnosis task 的 tools=[GetAlerts, NL2Metrics, NL2Traces, NL2Logs, NL2Kubectl]（无 MitigationCustomTool/Wait），mitigation task=[同上 + MitigationCustomTool, Wait]（crew.py:216-229）。grep 'a-lock|alock|rwlock|read.write.lock|readonly' 全仓库零命中（仅 streamlit_main.py 的 role 字段命中，无关）。
  - 这是软隔离：diagnosis 和 mitigation 共享 NL2Kubectl，而 NL2Kubectl 既能 get/describe 也能 apply/patch/delete/scale，所以 diagnosis 阶段在技术上是能写集群的（action_stack 会记录）。'只读观测 vs 写操作' 是靠 task 提示词+工具组合的约定，不是操作系统/K8s RBAC 层的硬权限。
- ❌ **没有所谓的 A-Lock 读写锁机制，代码里完全不存在。**
  - 证据：全仓库 grep (read_write.lock|rwlock|a.?lock|read.only|readonly|权限) 在 src/stratus 下没有任何相关命中。唯一的锁是 ActionStack._lock = threading.Lock()（action_stack.py:20），那是线程安全的单例栈锁，不是读写权限锁。
  - ActionStack._lock 是 thread.Lock，用于单例初始化（__new__ 里），与'读写权限隔离'无关。若简历/口头说'A-Lock 读写锁做权限隔离'，属不实或过度包装。
- ✅ **确定性控制流编排 = CrewAI Process.sequential + 写死的 task 列表，不存在动态 planner。**
  - 证据：crew.py:290-314 crew() 返回的 Crew 全部 process=Process.sequential；ITBench 与 AIOpsLab-mitigation 模式 agents/tasks 顺序固定。import: crew.py:17 from crewai import Agent, Crew, Process, Task。
  - 完全确定性、无 hierarchical、无动态路由。'确定性控制流'这个说法准确。
- ✅ **固定 task 顺序：mitigation 模式为 [Rollback(initial), initial_analysis(localization), Mitigation]；AIOpsLab 非 mitigation 为 [initial_analysis, diagnosis_tool]。Undo/Rollback 是第一步而不是最后一步。**
  - 证据：crew.py:295-300 (AIOpsLab mitigation): tasks=[sre_rollback_task(), initial_analysis_task(), sre_mitigation_task()]；crew.py:309-314 (ITBench): 同样 [rollback, initial_analysis, mitigation]。AIOpsLab 非 mitigation: crew.py:302-307 tasks=[initial_analysis_task(), sre_diagnosis_tool_task()]。tasks.yaml:32-39 sre_rollback_task 描述说把上一轮的修改全部撤销。
  - rollback 放第一步的设计目的是在每轮重试前把上一轮对集群的修改清空（tasks.yaml:34 'operations you performed in the previous round may negatively impact your current round'）。这与很多人直觉上'Undo 放最后'相反，面试要解释清楚：是 Round 内首步、不是 workflow 末尾。
- ✅ **不存在独立的 4 个 agent (Detection/Diagnosis/Mitigation/Undo)。实际只有 3 个真正 agent：sre_diagnosis_agent / sre_mitigation_agent / sre_rollback_agent。**
  - 证据：config/agents.yaml 只定义 3 个 role：sre_diagnosis_agent(1:1), sre_mitigation_agent(25:25), sre_rollback_agent(73:73)。crew.py 的 @agent 装饰器也只有这 3 个（crew.py:112/126/140）。没有 sre_detection_agent。
  - 'detection' 在代码里是 AIOpsLab 的一个 task_type 字符串（crew.py:88 docstring 列出 detection/localization/analysis/mitigation），不是 agent。detection 模式下复用 sre_diagnosis_agent（crew.py:302-303 用两个 sre_diagnosis_agent 跑 initial_analysis+diagnosis_tool）。若简历写'4 个智能体'，是把 task/phase 数错当成了 agent 数。
- ✅ **NL2Kubectl 实际通过 subprocess + shell=True 调用 kubectl 二进制执行命令，不是用 kubernetes client-python 调 API。**
  - 证据：tools/kubectl/kubectl.py:46 subprocess.run(command, shell=True, check=True, capture_output=True, input=input_data) # nosec B602；:131 dry_run 同样 subprocess.run(..., shell=True)。nl2kubectl.py:386-394 _execute_kubectl_command 调 KubeCtl.exec_command。NL2Kubectl 流程：nl2kubectl.py:137 _run → LLM 生成命令(:139) → _check_kubectl_command(:144 bashlex AST 校验禁止 pipe/redirect/复合命令) → KubeCtl.dry_run_json_output(:146) → _execute_kubectl_command(:154/176/181)。
  - kubernetes python 库确实 import 了（kubectl.py:10 from kubernetes import config），但只用于 config.load_kube_config()(:36) 加载 kubeconfig，CoreV1Api/AppsV1Api 那两行被注释掉(:37-38)。所以是'加载 kubeconfig + 走 kubectl 二进制'的混合，纯 API 调用路径未启用。
- ✅ **NL2Kubectl 做了命令安全校验：禁止管道、重定向、复合命令、交互式 flag，区分 safe/unsafe 命令白名单，执行写命令前先 server-side dry-run，成功写操作会生成回滚命令压入 ActionStack。**
  - 证据：nl2kubectl.py:34-110 kubectl_safe_commands / kubectl_unsafe_commands / kubectl_unsupported_commands / kubectl_dry_run_commands 四张表；_check_kubectl_command(:334-383) 用 bashlex 遍历 AST 禁止 pipe/redirect/compound/--interactive/-i/-t/-it；_run(:137-190) dry_run 后根据 DryRunStatus 分支；:171-184 use_rollback_stack 时 push rollback_command 到 action_stack。
  - forbid_unsafe_commands 开关默认 False（config.py:40），所以默认不会拦截 unsafe 命令，只校验 unsupported（pipe/交互式）。安全程度依赖运行时配置，不能默认说'严格白名单'。
- ✅ **分布式 Trace 由 NL2TracesCustomTool 读，且确实对接 Jaeger（经 Grafana datasource proxy 走 /jaeger/api/traces）。**
  - 证据：tools/grafana/nl2traces.py:107 url=f'{self.grafana_url}/jaeger/api/traces'；:155 /jaeger/api/services；:169 /jaeger/api/operations。:24 import JaegerLinter，:78-95 LLM 生成 jaeger function call。描述字面写 'gathering traces from jaeger'(:46)。
  - 经 Grafana unified datasource 代理访问 Jaeger（grafana_base_client.py:39 OBSERVABILITY_STACK_URL），不是直连 Jaeger 原生端口。还有个 GetFilteredTracesTool(nl2traces.py:298) 是 _distill_all_services_traces_pre_agent_run，确定性地抓所有服务 trace 并按 tag 过滤错误 span（http 500/error/otel.error/grpc 14），给 initial_analysis task 用。只在 ITBench 模式装配（crew.py:272）。
- ✅ **服务日志由 NL2LogsCustomTool 读，对接 Loki（经 Grafana datasource proxy）；集群资源状态由 NL2Kubectl(kubectl get/describe) 读，metrics 由 NL2MetricsCustomTool 读，对接 Prometheus。**
  - 证据：nl2logs.py:43 'access logs from Loki via the Grafana API'；:101 url=.../datasources/proxy/uid/{loki}/loki/api/v1/query_range。nl2metrics.py:43 'access metrics from Prometheus via the Grafana API'；:82 url=.../prometheus/api/v1/query。两个都带 Linter（LogQLLinter nl2logs.py:24, PromQLLinter nl2metrics.py:25）。集群状态则靠 NL2Kubectl get/describe（无独立 metrics-from-k8s 工具）。
  - 这三个 NL2* 工具仅在 ITBench 分支装配（crew.py:172-183, 216-229, 270-277）。AIOpsLab 模式下 traces/logs 走 GetTracesTool/ReadTracesTool/GetLogsTool（crew.py:161-164），数据来自 AIOpsLab generator 而非 Grafana；AIOpsLab 模式下 GetMetricsTool/ReadMetricsTool 被注释掉（crew.py:159,202），即 AIOpsLab 模式没有现成的 metrics 工具。
- ✅ **Retry/反思循环 (VALIDATION_RETRY 模式) 在 agent 外层（StratusAgentBase.run）用 while 循环 + reflection + oracle 验证驱动，crew 本身是单轮的。**
  - 证据：agent/base.py:177-289 run() 的 while not self.should_stop() 循环；:198 collect_reflection；:244-259 validation 成功就 submit 退出；config.py:14 默认 run_mode=VALIDATION_RETRY，max_retry_attempts=10。aiopslab.py:160-178 validate() 用 oracles（ClusterStateOracle 等）。
  - 所以'确定性控制流'指 crew 内的 task 顺序；而跨轮的 Retry/反思是 agent 外层循环，由 oracle 判定是否成功。两套机制要分开讲。
- ✅ **ActionStack 是线程安全的单例栈，存的是 RollbackNode(action, rollback_commands, cluster_state)，rollback 时按 LIFO 弹出执行回滚命令或 apply 保存的 yaml 快照。**
  - 证据：action_stack.py:16-39 单例+threading.Lock，push/pop/peek/clear。rollback_tool.py:46 RollbackTool._run(:258-304) pop 后遍历 last_action.rollback，command 类型调 KubeCtl.exec_command，file 类型调 _restore_cluster_state（按 Namespace/ConfigMap/.../Deployment 分层依赖顺序 apply，:105-171）。
  - 回滚快照是'执行写命令前'用 kubectl get -o yaml 抓的（nl2kubectl.py:192-210 _store_resource_state + :212-306 _gen_rollback_commands），依赖 dry-run 结果分类(created/deleted/autoscaled/patched)。use_rollback_stack 默认 True(config.py:44)。

### 核查：tnr
*逐条核实后的核心结论：这套"结构化变更策略"(ALLOWED_PATHS/ALLOWED_KINDS/ALLOWED_NAMESPACES + static_safety_check + check_test_preconditions + mutation_postcondition + build_inverse_patch + save_snapshot) 确实只存在于三个 scripts/ 下的沙箱/影子执行脚本 (controlled_execute_shadow_plan.py, dry_run_shadow_plan.py, shadow_plan_from_episode.py)，并且这三处是复制粘贴式的重复实现，namespace白名单还不一致 (controlled 只放 test-social-network，另两个放 4 个)。runtime 真正跑的 NL2KubectlCustomTool (src/stratus/tools/kubectl/nl2kubectl.py) 完全没有这套策略——它的 forbid_unsafe_commands 默认 False，_is_kubectl_command_safe 只做命令前缀白名单(只放行 get/describe/logs 等只读命令)，runtime 安全靠的是 kubectl 原生 server dry-run + 可选的前缀白名单 + 交互式命令 AST 检查 (_check_kubectl_command 禁 pipe/redirect/compound)，没有 JSON Patch test op、没有 precondition/postcondition。ActionStack.clear() 零调用者 (死代码)，push 由 nl2kubectl/exec_shell 调用，pop 由 RollbackTool 调用。runtime 快照是整对象 YAML (kubectl get -o yaml + 清理 resourceVersion/uid)，回滚靠 kubectl apply 整 YAML；inverse-patch (JSON Patch 反操作) 仅在沙箱脚本出现，runtime 不用。JSON Patch test op 的消费只发生在沙箱脚本 + schema/bandit 特征工程里，runtime 路径不消费。Oracle：ClusterStateOracle 委托 validate_cluster_status 做 pod-level 健康检查 (phase/容器 waiting/terminated/not-ready)，不查 replicas/endpoints；WorkloadOracle 跑 wrk2 压测看 Non-2xx。planner schema 字段齐备 (MitigationPlan/ActionPlan/JsonPatchOp/Condition)，但 Condition 的 preconditions/postconditions 在 runtime 不被执行，只被 offline_linucb 当奖励特征。planner :8008/plan 服务是外部 QLoRA 服务，eval_planner 只做离线 path/value 准确率评估，不执行 kubectl。*

- ✅ **结构化变更策略 (ALLOWED_PATHS/ALLOWED_KINDS/ALLOWED_NAMESPACES + static_safety_check + check_test_preconditions + mutation_postcondition + build_inverse_patch + save_snapshot) 只存在于沙箱脚本，runtime 工具 nl2kubectl.py 里没有这套策略。**
  - 证据：grep -rln 'static_safety_check|ALLOWED_PATHS|build_inverse_patch|mutation_postcondition|check_test_preconditions|save_snapshot|ALLOWED_KINDS|ALLOWED_NAMESPACES' 仅命中 scripts/dry_run_shadow_plan.py、scripts/shadow_plan_from_episode.py、scripts/controlled_execute_shadow_plan.py。src/stratus/tools/kubectl/nl2kubectl.py 全文无任何这些符号。
  - 这是简历最容易被夸大的点。沙箱脚本（shadow/gated-execution）才是结构化策略的真正落地点；runtime 真正执行的 NL2KubectlCustomTool 走的是另一套机制（见下条）。
- ✅ **三个沙箱脚本各自独立定义 ALLOWED_PATHS/ALLOWED_KINDS/ALLOWED_NAMESPACES，且常量内容不一致——是复制粘贴的重复实现，不是共享库。**
  - 证据：controlled_execute_shadow_plan.py:17-24 定义 ALLOWED_NAMESPACES={'test-social-network'}（只有 1 个）；dry_run_shadow_plan.py:23-28 namespace含 test/staging/prod/canary 四个；shadow_plan_from_episode.py:223-235 在函数内局部定义同样的四namespace集合。三处 ALLOWED_PATHS 内容一致（/spec/ports/0/targetPort、/spec/replicas、/spec/template/spec/nodeSelector、.../nodeSelector/kubernetes.io~1hostname）。
  - 面试时不要说'统一的策略模块'。这是三份近乎重复的脚本逻辑。controlled_execute 比 dry_run 保守（namespace白名单更窄）。
- ✅ **nl2kubectl.py 的 forbid_unsafe_commands 默认 False（即默认放行所有命令），_is_kubectl_command_safe 只做命令前缀白名单匹配。**
  - 证据：config.py:40-43 forbid_unsafe_commands: bool = Field(default=False)。nl2kubectl.py:148 仅当 config.forbid_unsafe_commands 为 True 才调用 _is_kubectl_command_safe。_is_kubectl_command_safe (nl2kubectl.py:328-332) 只是遍历 kubectl_safe_commands 列表（34-53，全是 get/describe/logs 等只读命令），startswith 命中即安全，否则不安全。
  - 默认关。这意味着默认配置下即便 agent 生成 kubectl patch/delete/scale 也不会被 _is_kubectl_command_safe 拦截。而且 patch/scale/delete 这些写命令本就不在 safe 列表里——只要这个开关没开，它们畅通无阻。
- ✅ **runtime NL2Kubectl 有 dry-run（KubeCtl.dry_run_json_output）和基于 dry-run 的分支执行，但没有 JSON Patch test op、没有 precondition/postcondition 校验、没有 ALLOWED_PATHS 路径白名单。**
  - 证据：nl2kubectl.py:146 对每条命令跑 server dry-run；:153-186 按 DryRunStatus(NOEFFECT/ERROR/SUCCESS) 分支。但 grep nl2kubectl.py 无 'test'/precondition/postcondition/ALLOWED/snapshot（除 _store_resource_state 外的快照）。dry-run 仅用 kubectl 自带 --dry-run=server，不解析 patch 的 test op。
  - runtime 的'安全'是 kubectl 原生 server dry-run + 可选前缀白名单 + 交互式命令 AST 检查（_check_kubectl_command:334-383 禁 pipe/redirect/compound/interactive flag），不是沙箱脚本那套结构化 JSON-Patch test/precondition/postcondition。
- ✅ **action_stack.py 的 clear() 没有任何调用者（死代码）。**
  - 证据：grep '.clear()' / 'action_stack.clear' / 'stack.clear' 在整个仓库（排除 .venv）对 action_stack 相关无命中。ActionStack.clear 定义于 action_stack.py:45-47，但无 caller。
  - push 由 nl2kubectl.py:184 和 exec_shell.py:38 调用；pop 由 RollbackTool(rollback_tool.py:269) 调用；peek 也无人调用；clear 完全未用。简历若说'实现并使用了 clear 做会话间状态重置'属夸大。
- ✅ **runtime 快照（_store_resource_state）存的是整对象 YAML（kubectl get -o yaml），并用 _cleanup_kubernetes_yaml 清掉 resourceVersion/uid/last-applied-configuration；回滚靠 kubectl apply 整 YAML，不是 inverse patch。**
  - 证据：nl2kubectl.py:192-210 _store_resource_state 执行 'kubectl get <type> <name> -o yaml'，经 _cleanup_kubernetes_yaml 写入 state_*.yaml，返回 RollbackCommand('file', state_file)。rollback_tool.py:276 _restore_cluster_state → _apply_resources_in_order（按 CRD 先后顺序 kubectl apply 整 YAML）。utils.py:189-215 _cleanup_kubernetes_yaml 递归删 metadata.resourceVersion、metadata.annotations.kubectl.kubernetes.io/last-applied-configuration、uid（保留 ownerReferences）。
  - runtime 没用 resourceVersion 做 optimistic concurrency，而是把 resourceVersion 直接抹掉再 apply（依赖 server 端最终一致）。inverse patch（JSON Patch 反操作）只在沙箱脚本 controlled_execute_shadow_plan.py:227-239 出现，runtime 没有。
- ✅ **沙箱脚本 build_inverse_patch 通过遍历 before 对象反推 JSON Patch，不依赖 resourceVersion；snapshot 存整对象 json+yaml 两份。**
  - 证据：controlled_execute_shadow_plan.py:227-239 build_inverse_patch：对原 patch 逆序，test op 跳过；replace/add → 若 before 存在该 path 则 replace/restore 原值，remove → add 原值；若 before 无该 path 则 remove。save_snapshot (:177-199) 同时写 {ns}_{kind}_{name}.before.json 和 .before.yaml。
  - 仅沙箱。这个 inverse 生成逻辑有局限：它假设每个 patch path 在 before 中要么存在要么不存在，对 list 索引（如 /spec/ports/0）可能脆弱，且不处理 test op 的值。
- ✅ **沙箱脚本用 JSON Patch 的 test op 做前置条件校验（check_test_preconditions），用 replace/add/remove 做后置条件校验（mutation_postcondition）；runtime 不使用 test op。**
  - 证据：controlled_execute_shadow_plan.py:149-165 check_test_preconditions：仅处理 op=='test'，比对 expected==get_json_path(live,path)。:202-224 mutation_postcondition：对 replace/add 校验 live 值==expected，对 remove 校验 path 已不存在。grep 显示 nl2kubectl.py 无 'test'/precondition/postcondition。schema.py:66 JsonPatchOp 定义 Literal['test','replace','add','remove']，并在 check_value_usage 强制 test/replace/add 必须带 value、remove 不带 value。
  - 前后置条件'到底在校验什么'：前置=test op 断言当前值=期望值（乐观锁式 gate），后置=断言变异后实际值=patch 期望值（成功确认）。这套语义只活在沙箱脚本 + schema 里，runtime 执行路径不消费它。
- ✅ **ClusterStateOracle.validate() 委托给 validate_cluster_status，检查 namespace 内每个 Pod 的 phase 与容器状态（CrashLoopBackOff/terminated/not-ready 即失败），不检查 Deployment replicas/Service endpoints。**
  - 证据：cluster_state.py:14-19 validate 调 validate_cluster_status(namespace)，success 决定 OracleResult。utils.py:83-169 validate_cluster_status：list_namespaced_pod，对每个 pod 跳过正在删除的，phase 不在 Running/Succeeded→失败；container_statuses 中 waiting(CrashLoopBackOff 单独标注)/terminated(reason!=Completed)/Running 但 not ready→失败。无 replicas/endpoints 逻辑。
  - validate_cluster_status 是 pod-level 健康检查，不是 workload-level。scale-to-zero 场景下若 pod 已被删光（list 为空），它会判定 success=True（没 pod = 没 issue）——这是个已知盲点，target-port/scale 类故障未必能被这个 oracle 抓到。
- ✅ **WorkloadOracle.validate() 跑 wrk2 压测（rate=10,dist=exp,conn=2,dur=10,threads=2），通过判断 wrk 输出含 'Requests/sec'/'Transfer/sec' 且无 'Non-2xx or 3xx responses' 来定 success；不解析具体 QPS 阈值。**
  - 证据：workload.py:113-149 validate：构造 Wrk(rate=10,dist='exp',connections=2,duration=10,threads=2)，对每个 workload(compose-post/read-home-timeline/read-user-timeline) 启 Job，取日志；若含 'Workload Generator Error' 或缺 'Requests/sec'/'Transfer/sec' 或含 'Non-2xx or 3xx responses' → success=False。
  - WorkloadOracle 是行为级 oracle（压测通过性），与 ClusterStateOracle（结构级）互补。它不卡延迟阈值，只看是否有非 2xx/3xx 与是否正常出报告。Social Network 3 个脚本、Hotel Reservation 1 个 mixed workload。
- ✅ **OracleResult 是 pydantic dataclass(success, message:dict)；OracleBase.passable 默认 False，validate 为 abstractmethod。**
  - 证据：oracle.py:7-19 @dataclass OracleResult(success:bool, message:dict)；OracleBase(BaseModel) passable:bool=Field(default=False)，@abstractmethod validate()->OracleResult。注释 'Assume that all oracles are sound because we cannot come up with a complete oracle at least for now.'
  - passable 默认 False 但 ClusterStateOracle/WorkloadOracle 都没覆盖它，且 validate 自己返回 OracleResult 不读 passable——passable 字段实际未被消费，属冗余设计字段。
- ✅ **Planner schema (planner/schema.py) 的核心模型字段：MitigationPlan/MitigationPlan/ActionPlan/JsonPatchOp/Condition 如下。**
  - 证据：schema.py:65-83 JsonPatchOp{op:Literal[test,replace,add,remove], path:str(必须以/开头), value:Any?} + 校验 remove 不带 value、test/replace/add 必须带 value。:86-91 Condition{name, resource?:ResourceRef, path?:str, operator:Literal[eq,ne,exists,non_empty,ready,oracle_pass]=exists, expected?}。:94-109 ActionPlan{tool='k8s_change', operation:OperationType, resource:ResourceRef, patch:list[JsonPatchOp]=[], risk:RiskLevel=MEDIUM, reason='', preconditions:list[Condition]=[], postconditions:list[Condition]=[], rollback:Literal[resource_snapshot,command,none]=resource_snapshot} + 校验 json_patch 必须有 patch。:120-133 MitigationPlan{schema_version='1.0', task_id, fault_type, root_cause:ResourceRef, evidence:list[EvidenceItem]=[], actions:list[ActionPlan], confidence:float=0.5[0,1]} + 至少一个 action。
  - 注意 schema 里 ActionPlan 默认 risk=MEDIUM、rollback=resource_snapshot，但沙箱脚本的 static_safety_check 要求 risk=='low' 才放行——所以 planner 若输出默认 MEDIUM 会被沙箱拒。schema 的 preconditions/postconditions 字段是 Condition 列表，但沙箱脚本实际用的是 patch 内的 test op，而非 ActionPlan.preconditions 列表——两套机制并存但语义重叠。
- ✅ **Condition.operator 支持 oracle_pass（把 oracle 结果作为条件），offline_linucb 会统计 preconditions/postconditions 数量做特征，但 runtime 不执行这些 Condition。**
  - 证据：schema.py:90 operator Literal 含 'oracle_pass'。planner/bandit/offline_linucb.py:185-186 preconditions_norm=min(len(action.preconditions),5)/5.0、postconditions_norm 同理，作为 LinUCB 特征(:209-210)；:116-124 对 patch test op 做归一化。但 src/stratus/ 下无任何代码消费 ActionPlan.preconditions/postconditions 去实际校验执行——只有 schema 定义 + bandit 特征工程。
  - Condition 列表是'设计完整、schema 已落地、但执行层未消费'的典型。面试若说'runtime 用 Condition 做 precondition gate'不实——runtime 是 nl2kubectl + kubectl dry-run，不读 Condition。LinUCB 只拿它当奖励特征维度。
- 🟡 **planner 服务（:8008/plan）是外部独立服务（QLoRA 模型），eval_planner.py 只做离线评估（patch path/value 命中率），不执行 kubectl。**
  - 证据：shadow_plan_from_episode.py:18 DEFAULT_URL='http://127.0.0.1:8008/plan'，scripts/call_planner_server.py 同样 POST 到该 URL。AIOpsLab/service.py:23 是 FastAPI app 但只暴露 /problems /agents /health /simulate（无 /plan）——/plan 服务在别处（QLoRA 推理服务，未见 server 启动代码在仓库内）。eval_planner.py 全文无 subprocess/kubectl/execute，仅对比 patch_paths/patch_values 做准确率（:98-99 patch_path_acc/patch_value_acc），并定义 DANGEROUS_OPS={delete,create,apply_yaml}(:17) 用于风险评估而非拦截。
  - planner server 本身的启动入口（serve /plan 的 FastAPI/uvicorn）在仓库里没找到——只找到 client（call_planner_server/shadow_plan_from_episode）和 eval 脚本。若简历声称'实现了 planner 推理服务'需确认服务代码位置；仓库内可验证的是 schema、QLoRA 训练(train_qlora.py)、离线 eval、LinUCB bandit。
- ✅ **runtime 回滚（RollbackTool）通过 action_stack.pop() 取出 RollbackNode，按 command_type 分发：'command' 直接 exec，'file' 调 _restore_cluster_state（kubectl apply 整 YAML）；可选 validate_rollback 用 unified_diff 比对 namespace 状态。**
  - 证据：rollback_tool.py:263-318 _run：action_stack 为 None→告警；pop() 得 last_action；遍历 last_action.rollback，command_type=='command'→KubeCtl.exec_command，'file'→_restore_cluster_state(:86→_apply_resources_in_order 先 CRD 后普通)。若 config.validate_rollback(:292-304) 则 sleep+get_namespace_state+compare_states(unified_diff) 写 rollback_diff/ref 文件。
  - validate_rollback 默认 False(config.py:49)。所以默认回滚不做 diff 验证。runtime 回滚是'整对象 apply'式，不是 inverse-patch 式——与沙箱脚本 controlled_execute 的 build_inverse_patch 是两条独立实现路线。

### 核查：benchmark
*核查 K8s 多智能体 SRE 系统的 benchmark 领域。核心结论：故障注入/负载回放/Agent执行/环境复位/指标汇总这条端到端链路是真实存在的，但主要由 AIOpsLab 的 Orchestrator 驱动（init_problem 注入故障 + 启动 wrk 负载，start_problem 跑 agent 循环并在结束时 recover_fault + cleanup + app.cleanup 卸 namespace），stratus/main.py 只是 BENCHMARK=AIOpsLab 下的薄封装调用 Orchestrator，并非自己重写了编排。wrk 负载回放在 stratus/tools/oracle/workload.py（基于 AIOpsLab 的 Wrk，对 Social Network / Hotel Reservation 两类 app 打 lua 脚本；astronomy_shop 被显式跳过）。轨迹记录确实存在但分层：live 轨迹由 STRATUS/CrewAI 在 test_agent.sh 里 tee run.log，再由 evolution/extract_trajectory.py 后处理解析成 episodes/steps/resource_diffs.jsonl；controlled_execute_shadow_plan.py 是沙箱脚本，记录 patch/回滚/postcondition 到 controlled_exec_report.json + action_stacks 快照目录；export_trajectory_rewards.py 把三阶段报告聚合成 trajectory_events/reward_events JSONL。DeathStarBench 提供底层微服务镜像与 wrk lua 脚本（social-network=test-social-network，hotel-reservation=test-hotel-reservation，astronomy-shop=astronomy-shop）。Kind 通过 test_agent.sh 的 setup_cluster() 在每个 task 前重建集群（kind delete + kind create --config AIOpsLab/kind/kind-config-{arch}.yaml），有 x86/arm 两份配置。配置类故障端到端闭环验证了 3 个故障族：targetPort mismatch、deployment scaled to zero、nodeSelector 指向不存在节点，均有 live episode + 沙箱 controlled execution + LinUCB replay 产物，但 controlled execution 和 LinUCB 都是沙箱/离线 replay，尚未接回 live AIOpsLab Oracle reward。需要警惕：LinUCB 的奖励是脚本合成的 shaped label（不是真实 Oracle），replay 明确注释“not an online bandit update inside live AIOpsLab”。*

- ✅ **故障注入由 AIOpsLab Orchestrator.init_problem 驱动（注入 + atexit 注册恢复 + 启动负载），stratus/main.py 在 BENCHMARK=AIOpsLab 时只是薄封装调用 Orchestrator**
  - 证据：stratus/src/stratus/main.py:58-88（if BENCHMARK==AIOpsLab: orchestrator=Orchestrator(); init_problem; register_agent; asyncio.run(start_problem(max_steps=30))）；AIOpsLab/aiopslab/orchestrator/orchestrator.py:33-88（init_problem 内 prob.inject_fault():75、atexit.register(exit_cleanup_fault):76、prob.start_workload():79-82）
  - 编排逻辑完全属于 AIOpsLab 上游，stratus 侧没有自己重写串联逻辑。如果面试说'我自研了自动串联'，应改口为'复用 AIOpsLab Orchestrator 做 init_problem/start_problem/register_agent'。
- ✅ **wrk 负载回放由 stratus/tools/oracle/workload.py 的 WorkloadOracle 实现，对 Social Network / Hotel Reservation 两类 app 打 lua 脚本**
  - 证据：stratus/src/stratus/tools/oracle/workload.py:49-63（get_workloads：Social Network → socialNetwork/wrk2/scripts 三个 lua；Hotel Reservation → hotelReservation/wrk2/scripts/mixed-workload_type_1.lua）；validate():113-149 用 Wrk(rate=10,dist=exp,connections=2,duration=10,threads=2) 创建 wrk2 Job
  - WorkloadOracle 在 main.py:73-78 仅对 detection/mitigation 且非 astronomy_shop 任务启用。wrk 是 AIOpsLab 提供的 Wrk 类，stratus 包装成 Oracle。底层 lua 脚本来自 DeathStarBench 的 TARGET_MICROSERVICES 目录。
- ✅ **环境复位由 Orchestrator.start_problem 末尾完成：recover_fault + atexit.unregister + app.cleanup() + prometheus.teardown() + 删 OpenEBS**
  - 证据：AIOpsLab/aiopslab/orchestrator/orchestrator.py:194-209（with CriticalSection: self.session.problem.recover_fault()；atexit.unregister；self.session.problem.app.cleanup()；self.prometheus.teardown()；kubectl delete sc openebs）
  - 复位是 Orchestrator 自带，注释 199-201 明确说'删除整个 namespace 更安全但更慢'，当前用 app.cleanup() 而非整 namespace 删除。
- ✅ **指标汇总/最终评测由 Orchestrator 调 session.problem.eval() 产生，并可选写 wandb**
  - 证据：AIOpsLab/aiopslab/orchestrator/orchestrator.py:184-192（results=self.session.problem.eval(solution, history, duration)；self.session.to_json()；if self.use_wandb: self.session.to_wandb()），TTD/TTL/TTA/TTM 与 framework_overhead 计算:213-218
  - 指标汇总属于 AIOpsLab session.eval，stratus 侧没有额外自定义聚合；use_wandb 默认 false（orchestrator.py:31）。
- ✅ **统一记录工具调用/K8s diff/回滚/Oracle 形成可追溯轨迹，写在 evolution/trajectories/*.jsonl**
  - 证据：stratus/evolution/extract_trajectory.py:506-635（extract_steps 解析 NL2Kubectl Tool prompt/command/dry-run/execution/rollback/submit → TrajectoryStep）；:361-418（extract_targetport_diffs 提取 before/after/verified_9090）；main():693-733 写 episodes.jsonl/steps.jsonl/resource_diffs.jsonl/summary.json。已存在产物 evolution/trajectories/episodes.jsonl、steps.jsonl、resource_diffs.jsonl（实测 resource_diffs 含 before:9999 after:9090 verified_9090:true）
  - 这是离线后处理脚本（解析 CrewAI 的 run.log），不是实时在线记录。轨迹来源是 test_agent.sh tee 出的 run.log。steps.jsonl 记录 tool_call/k8s_write/rollback/oracle/format_retry 事件类型（extract_trajectory.py:523-599）。
- ✅ **controlled_execute_shadow_plan.py 记录 patch/快照/回滚/postcondition 到 controlled_exec_report.json + action_stacks 目录**
  - 证据：stratus/scripts/controlled_execute_shadow_plan.py:252-253（out_path=eval_dir/stratus_output/controlled_exec_report.json）；save_snapshot():177-199 写 {namespace}_{kind}_{name}.before.json/.yaml 到 stack_dir；:310-312 stack_dir=action_stack_dir/controlled_exec_{now}_{eval_dir.name}；build_inverse_patch():227-239 构造回滚 patch；实测 action_stacks/controlled_exec_2026-06-21_110746_* 目录存在
  - 沙箱默认 execute_real 未设则只 dry-run 不真改（:319-323 execute_real_flag_not_set）。ALLOWED_PATHS/KINDS/NAMESPACES 严格白名单（:17-24），且 namespace 只允许 test-social-network，故 controlled execution 只验证了 social-network namespace。
- ✅ **export_trajectory_rewards.py 把 shadow/dry-run/controlled 三阶段报告聚合成 trajectory_events + reward_events JSONL**
  - 证据：stratus/scripts/export_trajectory_rewards.py:278-349（build_events_for_episode 读 shadow_planner_out/shadow_safety_report/shadow_dry_run_report/controlled_exec_report）；main():382-411 写 trajectory_events_{stamp}.jsonl / reward_events_{stamp}.jsonl / reward_summary.json / reward_config.json。实测 artifacts/reward_events/reward_events_2026-06-21_212539.jsonl 共 21 条（shadow 9 + dry_run 9 + controlled 3；family targetport7/scale7/assign7）
  - reward 是 shape 出来的代理奖励（REWARD_CONFIG:30-56，base_if_postcondition_passed=1.0 等），不是 live Oracle 终态奖励。shadow 阶段 reward_shadow() 注释明确 'not a real execution reward'（:190）。
- ✅ **DeathStarBench 提供 social-network / hotel-reservation / astronomy-shop 微服务，namespace分别为 test-social-network / test-hotel-reservation / astronomy-shop**
  - 证据：AIOpsLab/aiopslab/service/metadata/{social-network,hotel-reservation,astronomy-shop}.json 实测 Namespace 字段：test-social-network、test-hotel-reservation、astronomy-shop；对应 app 实现文件 socialnet.py / hotelres.py / astronomy_shop.py（service/apps/）；workload.py:51-61 wrk lua 路径指向 socialNetwork/wrk2 与 hotelReservation/wrk2（DeathStarBench 仓库结构）
  - DeathStarBench 是底层数据/微服务来源，stratus 自身代码没有直接 clone/构建 DeathStarBench 的脚本，靠 AIOpsLab 的 TARGET_MICROSERVICES 引用。astronomy_shop 在 main.py:74-81 被 WorkloadOracle 显式跳过（不跑 wrk）。
- ✅ **Kind 通过 test_agent.sh setup_cluster() 在每个 task 前重建集群（kind delete + kind create --config），有 x86/arm 两份配置**
  - 证据：stratus/test_agent.sh:58-63（setup_cluster(): kind delete cluster --name kind; kind create cluster --config ./AIOpsLab/kind/kind-config-${arch}.yaml）；:101-103 非 preserve_cluster 时每个 task 自动调用 setup_cluster；AIOpsLab/kind/kind-config-x86.yaml、kind-config-arm.yaml 存在；load_images.sh（kind load docker-image）；eval/eval.py:10 按 arch 选 kind_config
  - Kind 配置/脚本都在 AIOpsLab/kind/ 子目录（上游资产），stratus 没有自己写 kind 配置。kind-config-arm.yaml 有 .bak 备份文件（2026-06-17 曾改过）。集群名固定 'kind'。
- ✅ **配置类故障端到端闭环验证了 3 个故障族：targetPort mismatch、deployment scaled to zero、nodeSelector 指向不存在节点**
  - 证据：stratus/eval/ 下有 11 个 target_port、4 个 scale_pod_zero、3 个 assign_to_non_existent_node 的 live episode 目录；export_trajectory_rewards.py:18-28 DEFAULT_EPISODES 正是这 3 族各 3 条；controlled exec manifest（controlled_exec_sandbox_3families_2026-06-21_111723.manifest.txt）列出 3 条 controlled_exec_report.json 覆盖 3 族；CANONICAL_FAULTS（multifault_linucb_replay.py:21-25）锁定这 3 族
  - '端到端闭环'指 live STRATUS 修复已跑通 + QLoRA 阶段到 sandbox controlled execution。但 QLoRA gated execution 仍未接回 live AIOpsLab（safeops_final_milestone_table 第4节'严格结论'自承）。规模小：每族约 3 条 episode。
- ✅ **三故障族 controlled execution：3/3 real_executed + patch/postcondition/rollback 全成功**
  - 证据：artifacts/planner_server/trajectory_reward_summary_2026-06-21.md:62-69（static_safe 3/3, precondition_matches 3/3, real_executed 3/3, patch_succeeded 3/3, postcondition_passed 3/3, rollback_executed 3/3, rollback_succeeded 3/3）；controlled_execution_reward n=3 mean=0.8
  - 这是 sandbox 环境（Kind + ALLOWED_NAMESPACES={test-social-network}）。rollback 是 demo 用的 'rollback-after-success'（controlled_execute_shadow_plan.py:345-347 rollback_after_success 标志），即成功后也回滚以便复用沙箱。
- ✅ **Multi-fault LinUCB hard-mode replay：random 17.04% / min-risk 33.56% / LinUCB 73.68%，unsafe=0，覆盖 570 episodes**
  - 证据：artifacts/planner_server/multifault_linucb_hardmode_summary_2026-06-21.md:42-66；stratus/scripts/multifault_linucb_replay.py:561-645（evaluate_linucb 实现）；artifacts/bandit_results/multifault_linucb_replay_summary_2026-06-21_214157.json 存在
  - 这是 OFFLINE replay，奖励是 generate_candidates() 合成的 shaped label（reward 1.0/-0.8/0 等，replay.py:282-356），不是 live Oracle。脚本 notes 第3条明确 'live Oracle reward integration is the next stage'（:776-779）。hard-mode 是手工把候选 risk 都压成 low 防泄露（:344-354）。
- ✅ **LinUCB candidate 生成是脚本合成的（canonical/wrong-value/wrong-kind/rollout-restart/merge/delete 等候选），并非真实 agent 产生的多候选**
  - 证据：stratus/scripts/multifault_linucb_replay.py:282-356（generate_candidates：chosen_json_patch reward 1.0、wrong_patch_value -0.8、wrong_resource_kind -0.6、rollout_restart_decoy 0.0、merge_patch_decoy -0.25、rejected_delete_resource -1.0 等手工造的候选）
  - 面试若说'用 LinUCB 在线从 agent 多个修复方案里选最优'不准确——候选与奖励都是合成/标注的，目的是验证 bandit 排序能力原型。真实多候选+真实奖励的在线版本未实现。
- ✅ **live STRATUS 修复轨迹的真实 Oracle 验证：targetPort 修复 verified_9090、endpoints 9090**
  - 证据：stratus/evolution/trajectories/resource_diffs.jsonl 实测首行：before:9999, after:9090, verified_9090:true, observed_endpoints 含 9999→9090 变化；extract_trajectory.py:354-358 targetport_verified 正则校验 TargetPort:9090/TCP + Endpoints:*:9090
  - 这是真 live Kind 轨迹（source_log 指向 artifacts/stability/.../eval-run.log）。但只有 targetPort 族做了这种Endpoint级 diff 校验；scale/assign 族的 resource_diffs 校验较弱（extract 主要按 targetPort 写的）。
- 🟡 **STRATUS 多阶段控制流（Diagnosis/Mitigation/Undo/Validation Retry）在原 runtime 中存在**
  - 证据：stratus/src/stratus/main.py:24-36（RUN_MODE: NAIVE / BLINDLY RETRY TODO / VALIDATION_RETRY 默认，MAX_RETRY_ATTEMPTS、SLEEP_BETWEEN_RETRIES、VALIDATION_WAIT_TIME）；agent/aiopslab.py:160-178 validate() 跑 oracles
  - VALIDATION_RETRY 默认模式 + Oracle 校验存在，但 BLINDLY RETRY 仍标 TODO（main.py:25）。safeops_final_milestone_table 第38行自承'确定性控制流编排 Diagnosis/Mitigation/Undo 部分完成，下一步是把 STRATUS 改 read-only diagnosis + gated write'。

### 核查：finetuning
*A real, complete QLoRA training+eval pipeline exists (planner/train_qlora.py, eval_planner.py): Qwen2.5-7B, NF4 4-bit QLoRA via bitsandbytes+peft, rank16/alpha32, 7 linear target modules, paged_adamw_8bit, transformers.Trainer (NOT TRL/SFTTrainer). SFT and Tool-Calling datasets exist and use the standard messages=[{role,content}] and messages=[{role,tool_calls}]+tools formats. HOWEVER: (1) adapters/ is empty and there is NO train_metrics.json/eval_metrics.json/adapter_config.json/safetensors anywhere, so no checkpoint was actually produced in this repo — training was written and py_compiled but never run to completion here. (2) The "Tool-Calling data from Oracle trajectory + failure-correction samples" framing is partly design-only: data is built by deterministic template synthesis (build_tool_data.py / build_multifault_data.py), tied to verified episode IDs but NOT extracted from agent run logs; the real Oracle-trajectory parser (extract_trajectory.py) emits TrajectoryStep events for analysis, not SFT pairs. (3) LinUCB is fully implemented and real but is OFFLINE REPLAY over synthetically-shaped candidate rewards (chosen +1.0, decoys negative), explicitly NOT online and NOT using live Oracle rewards. (4) "22 features" refers to the targetPort offline_linucb.py; the multifault replay uses a 39-dim feature set. Reward shaping (export_trajectory_rewards.py) genuinely combines terminal Oracle success + execution cost + rollback penalty, but only on 9 episodes (3 controlled-exec), and that reward is NOT fed into either the SFT or the bandit pipeline yet.*

- ✅ **存在真实的 QLoRA 训练脚本 planner/train_qlora.py，能跑完整 train+save+evaluate**
  - 证据：planner/train_qlora.py:145-262 main(): BitsAndBytesConfig(load_in_4bit=True, nf4) -> AutoModelForCausalLM -> prepare_model_for_kbit_training -> LoraConfig -> get_peft_model -> Trainer(...).train() -> trainer.save_model(output_dir) -> tokenizer.save_pretrained -> trainer.evaluate()，并写 train_metrics.json / eval_metrics.json。脚本结构完整可执行。
  - 脚本是真实的、完整的，且经 py_compile 通过（write_qlora_training_files.sh:565 python -m py_compile）。但仅此而已——见下条：repo 里没有任何 checkpoint 产物。
- ✅ **LoRA 配置：rank=16, alpha=32, dropout=0.05, bias=none, task_type=CAUSAL_LM**
  - 证据：planner/train_qlura.py:161-163 --lora_r default=16, --lora_alpha default=32, --lora_dropout default=0.05；LoraConfig(...) bias="none", task_type="CAUSAL_LM" (line 209-224)。
  - 这些是 argparse 默认值；实际跑训练时是否被覆盖未知（因为没有 checkpoint/metrics 留存）。
- ✅ **target_modules 覆盖 7 个线性层 q/k/v/o + gate/up/down_proj（全量 attention+MLP）**
  - 证据：planner/train_qlora.py:215-223 target_modules=["q_proj","k_proj","v_proj","o_proj","gate_proj","up_proj","down_proj"]。
  - 这是 7 个线性层的标准 QLoRA target，没有 Gate/MLP 之外的特殊设计。
- ✅ **是 QLoRA(NF4 4bit) + double quant，compute dtype bf16/fp16 自适应**
  - 证据：planner/train_qlora.py:192-197 BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_use_double_quant=True, bnb_4bit_compute_dtype=compute_dtype)；compute_dtype 由 bf16_supported 决定 (line 172-174,190)。
  - 标准 QLoRA 配置，无意外。optim=paged_adamw_8bit (line 165)，gradient_checkpointing=True (line 128)。
- ✅ **训练框架是 transformers.Trainer + 自写 Dataset/Collator，不是 TRL/SFTTrainer**
  - 证据：planner/train_qlora.py:13-19 from transformers import ... Trainer, TrainingArguments；line 28 自写 ChatSFTDataset(Dataset)，line 83 自写 DataCollatorForCausalSFT；line 235 trainer = Trainer(...)。grep -rn 'SFTTrainer|from trl|import trl' 全仓（排除 .venv）0 命中。.venv site-packages 中无 trl（只有 transformers/peft/torch/bitsandbytes）。
  - 若简历/口语稿说"用 TRL SFTTrainer"则不实——是裸 transformers.Trainer + 手写 labels 掩码（line 72-74 prompt 部分 labels=-100）。这是等价但更底层的实现。
- ✅ **SFT 数据格式是 messages=[{role,content}]，assistant 用 content（纯 JSON 文本），不用 tool_calls**
  - 证据：planner/data/sft_train.jsonl 每行 {id, messages:[{role:system,content},{role:user,content},{role:assistant,content:"<MitigationPlan JSON>"}]}。train_qlora.py:42-44 显式取 messages[-1].get('content')，line 44-45 raise ValueError('SFT row must use assistant content, not tool_calls')。
  - SFT 通道确实是纯 content。Tool-Calling 训练数据是另一套文件（见下条），train_qlora.py 目前只消费 sft_*.jsonl，并不直接消费 toolcall_*.jsonl。
- ✅ **Tool-Calling 数据格式是 messages=[{role,tool_calls:[{type:function,function:{name,arguments}}]}] + 顶层 tools 字段，正是 OpenAI tool-calling schema**
  - 证据：planner/data/toolcall_train.jsonl 行含 assistant.tool_calls=[{type:"function",function:{name:"generate_mitigation_plan",arguments:"<plan JSON>"}}] 及顶层 tools=[{type:function,function:{name,description,parameters:MitigationPlan.model_json_schema()}}]。构造代码 evolution/build_multifault_data.py:105-136 make_toolcall_sample()。
  - 格式正确且真实存在。但注意：这套 toolcall_*.jsonl 目前没有任何训练脚本消费它——train_qlora.py 只读 sft_*.jsonl。toolcall 数据是"造好了但管线没接上"。
- 🟡 **Tool-Calling 训练数据"从 Oracle 轨迹 + 失败纠错样本"构造**
  - 证据：实际构造器是 evolution/build_tool_data.py（targetPort）与 evolution/build_multifault_data.py（3 故障族）。两者都是按 verified episode 的 namespace/deployment/node 等参数，用 make_*_plan() 模板"合成"MitigationPlan（build_multifault_data.py:498-563 synthetic_*_samples），再 make_sft_sample/make_toolcall_sample 包装。失败的 rejected/纠错样本由 make_delete_rejected / make_wrong_patch_value 等模板生成（build_multifault_data.py:385-412, offline_linucb.py:230-322），并非从 agent run.log 解析失败轨迹。
  - "Oracle 轨迹"真实存在但是另一条管线：evolution/extract_trajectory.py 解析 agent run.log 产出 TrajectoryStep 事件（event_type 含 oracle/k8s_write/tool_call，line 526/594/688），只用于分析/打 reward，不直接产 SFT pair。数据通过 verified episode 的 episode_id（如 verified::06-18_...）与真实跑通的 Oracle 成功 episode 关联，但 plan 正文是模板生成的，不是从 agent 输出里抠出来的。dataset_summary.json by_source 显示绝大部分是 synthetic_from_verified_*_template（240+160+160）。诚实表述：数据是"以验证通过的 Oracle 成功 episode 为锚、参数化模板扩增"，不是"从原始轨迹逐条抽取"。
- ❌ **真正微调出了 checkpoint（adapter 权重）**
  - 证据：planner/adapters/ 目录为空（ls -la 仅 . 和 ..）。全仓 find adapter_config.json / *.safetensors / train_metrics.json / eval_metrics.json（排除 .venv）均 0 命中。
  - 训练脚本能跑、py_compile 通过，但本 repo 里没有产出的 checkpoint/metrics。若面试说"训出了 Qwen2.5-7B QLoRA adapter"需诚实说明：脚本就绪、数据就绪，但产物未提交/未在本环境留存（或训练未真正跑完）。trajectory_reward_summary 里提到的 'A800 QLoRA Planner Server' 是推理服务侧表述，不等于训练 checkpoint 在此。
- ✅ **LinUCB：A=λI 初值，A_inv=np.linalg.inv(A)，theta=A_inv@b，UCCB=theta·x + alpha·sqrt(x·A_inv·x)**
  - 证据：scripts/multifault_linucb_replay.py:567 A=lambda_reg*np.eye(d)；line 580 A_inv=np.linalg.inv(A)；line 581 theta=A_inv@b；line 586 bonus=alpha*sqrt(max(0,x@A_inv@x))；line 587 score=mean+bonus。更新 line 603-604 A+=np.outer(x,x); b+=r*x。planner/bandit/offline_linucb.py:370-389 用等价的 np.linalg.solve 求解（theta=solve(A,b), Ax=solve(A,x)）。
  - 标准 LinUCB 实现，两处等价（一处显式求逆、一处用 solve）。lambda_reg 默认 1.0。
- ✅ **候选 arm 是"同一个故障的不同修复 plan"（7 类候选，chosen+6 个 decoy）**
  - 证据：scripts/multifault_linucb_replay.py:282-356 generate_candidates() 产出 7 个 candidate_id：chosen_json_patch(+1.0)/missing_test_precondition(+0.45)/wrong_patch_value(-0.80)/wrong_resource_kind(-0.60)/rollout_restart_decoy(0.0)/merge_patch_decoy(-0.25)/rejected_delete_resource(-1.0, 被 policy block)。offline_linucb.py:336-367 build_candidates() 是 6 类（targetPort 版）。
  - 候选不是"不同 plan 文档"，而是对同一个 canonical 修复做扰动生成的 7 个变体（错值/错资源/缺前置/重启诱饵/merge 诱饵/危险删除）。本质是 contextual-bandit 在"安全过滤后的候选"上做 ranking。
- ✅ **奖励来自"终态 Oracle 成功 + 执行成本 + 回滚惩罚"**
  - 证据：这描述的是 scripts/export_trajectory_rewards.py 的 controlled_execution 奖励：line 233 base_if_postcondition_passed=1.0；line 247-251 execution_cost_penalty=0.05*(real_executed+rollback_executed)；line 252-257 rollback_executed -0.10、rollback_failed -0.50；line 288 historical_oracle_success 由 parse_oracle_success() 从 run.log 抓 SUCCESSFUL/FAILED (line 138-156)。这是真实的多分量奖励。
  - 但这是 trajectory reward 管线（reward_controlled），与 LinUCB 管线目前是断开的。LinUCB 用的 reward 是 generate_candidates 里硬编码的 shaped 标签（±1/0.45/-0.8 等），不是 export_trajectory_rewards 算出的执行奖励。reward_summary_latest 显示 controlled_execution 只有 3 条（3 个 episode），数据量极小。两套奖励尚未对接。
- ✅ **LinUCB 是离线重放，不是在线学习**
  - 证据：scripts/multifault_linucb_replay.py:775-779 notes: 'offline replay over generated multi-fault candidate plans, not an online bandit update inside live AIOpsLab'；main() 对同一 episodes 用 20 seeds×5 alphas 反复重放求统计。offline_linucb.py:524-527 scope: 'Offline replay ... not live AIOpsLab Oracle rewards.' README_multifault_linucb.md:31-33 同。
  - 代码里确实有 bandit.update()（online-style 更新 A/b），但因为是固定数据集反复跑、且 reward 是预设 shaped 标签，实质是离线策略评估/重放，不是真在线。README/注释都诚实标注了。
- ✅ **"22 个特征"——LinUCB 上下文特征维度**
  - 证据：planner/bandit/offline_linucb.py:17-40 FEATURE_NAMES 恰好 22 个：bias, diagnosis_alignment, op_json_patch, op_rollout_restart, op_merge_patch, op_delete, op_create_or_apply, resource_is_service, resource_name_match, namespace_match, has_targetport_replace, targetport_replace_value_match, has_targetport_test, targetport_test_value_match, patch_field_count_norm, action_count_norm, rollback_snapshot, risk_low, risk_medium, risk_high_or_blocked, preconditions_norm, postconditions_norm。feature_vector() line 138-213 逐个计算。
  - 重要区分：'22 特征'是 targetPort 单故障版的 offline_linucb.py。multifault_linucb_replay.py 是另一套 39 维特征（FEATURE_NAMES line 30-44，含 risk/op/kind/fault/path align/value/precondition/rollback/unsafe_like 等）。若面试说"LinUCB 用 22 特征"要说明是哪个版本——multifault 版是 39 维。两套特征定义不一致（不是子集关系）。
- ✅ **Planner schema（MitigationPlan）与 LinUCB 特征的关系：特征从 ActionPlan/MitigationPlan 字段派生**
  - 证据：planner/schema.py:94-109 ActionPlan(tool,operation,resource,patch,risk,reason,preconditions,postconditions,rollback)；:120-133 MitigationPlan(task_id,fault_type,root_cause,evidence,actions,confidence)。offline_linucb.py feature_vector() 从 plan.actions[0] 的 operation/risk/resource/patch/preconditions/postconditions/rollback 派生 22 特征；multifault 版同理从同一 schema 字段派生 39 特征。
  - 特征是 schema 字段的工程化 one-hot/alignment 编码，不是模型 embedding。diagnosis_alignment 是若干布尔特征的均值（offline_linucb.py:171-181）。Planner schema 与特征一一对应、强耦合。
- ✅ **LinUCB 离线重放结果：best alpha=1.0，selected_correct_rate≈73.7%，远超 random(17%)/min_risk(33.6%)**
  - 证据：artifacts/bandit_results/multifault_linucb_replay_summary_*.json best_alpha=1.0, episodes=570；artifacts/planner_server/multifault_linucb_hardmode_summary_2026-06-21.md:42-66 random 17.04%/min_risk 33.56%/LinUCB 73.68% selected_correct_rate，unsafe_selected_rate=0。
  - 数字真实、有 per_seed CSV + summary JSON 留存。但这是 hard-mode（强制所有非破坏候选 low risk 去泄漏）下的离线重放结果，reward 是 shaped 标签，不能等同于真实 K8s 修复成功率。README 明确 live Oracle 集成是 next stage。
- ✅ **存在偏好对(preference_pairs)/DPO 数据与脚本**
  - 证据：planner/data/preference_pairs.jsonl(244行) 与 planner/data_multifault/preference_pairs.jsonl(570行)；schema.py:184-190 PreferencePair(pair_id,prompt,chosen:MitigationPlan,rejected:MitigationPlan,reason)。构造 build_multifault_data.py:566-601 build_preference_pairs()，rejected=make_delete_rejected（危险删除）。
  - 数据真实存在。但全仓没有 DPOTrainer/DPO 训练脚本（grep DPOTrainer 0 命中）。偏好对目前主要被 LinUCB 重放当候选源用（offline_linucb.py:336 读 pair 的 chosen/rejected），尚未用于真正的偏好优化训练。是"数据就绪、训练未接"。
- ✅ **训练用基座模型是 Qwen/Qwen2.5-7B-Instruct**
  - 证据：planner/train_qlora.py:148 default model_name_or_path='Qwen/Qwen2.5-7B-Instruct'；eval_planner.py:404 同。output_dir default 'planner/adapters/qwen25-7b-targetport-qlora'。
  - 默认值如此。data/ 是 targetPort 单故障 sft（198 train），data_multifault/ 是 3 故障族（462 train）。脚本默认指向 data/sft_train.jsonl（targetPort 版），跑 multifault 需手动 --train_file planner/data_multifault/sft_train.jsonl。

### 核查：jaeger
*面试"Jaeger 与分布式链路追踪"fact sheet。所有通用事实已用 WebSearch 查证（CNCF 官方公告、Jaeger 官方文档、W3C trace-context 规范、OpenTelemetry 官方 sampling/context-propagation 文档），项目落地部分基于本仓库真实代码（nl2traces.py、read_traces.py、get_traces.py、jaeger_linter.py）。核心叙事：Jaeger 是 CNCF 毕业的分布式追踪后端（Uber 2015，2019 毕业），解决"一个请求穿几十个微服务，慢/错在哪一跳"的问题；OpenTelemetry 是采集标准（SDK + OTLP），Jaeger 是存储+UI 后端；trace 由 span 通过 parent/child 组成调用树，靠 HTTP header（W3C traceparent / B3）跨进程透传；采样分 head-based（入口决定，便宜但可能漏掉错误）和 tail-based（看完所有 span 再决定，能保错误/慢请求但贵）。在这个项目里，DeathStarBench 微服务被注入故障 → 请求链路里某 span 变慢/报错 → NL2Traces 把"哪个服务/哪一跳/trace id"翻译成 /jaeger/api/traces 查询（可按 error=true / http.status_code=500 / otel.status_code=ERROR / rpc.grpc.status_code=14 过滤）→ LLM 摘要 + 父子 span 回溯到叶子节点 → 给 Diagnosis agent 定位根因。这是"分布式 Trace"数据通路的落地。*

- ✅ **Jaeger 是一个开源分布式追踪平台（distributed tracing platform），由 Uber 于 2015 年创建，2016 年开源后捐赠给 CNCF，2019 年 10 月从 CNCF 毕业（graduated project），定位是 OpenTelemetry 的存储 + 可视化后端。**
  - 证据：CNCF 官方公告 'Cloud Native Computing Foundation announces Jaeger graduation'（2019-10-31）：Jaeger created in 2015 by Uber，accepted as CNCF Incubation project Sep 2017，graduated Oct 2019。GitHub repo 'jaegertracing/jaeger' 自述 'a Distributed Tracing Platform created by Uber Technologies and donated to Cloud Native Computing Foundation'。
  - 面试可能追问的考点：(1) Jaeger 灵感来自 Google 2010 年的 Dapper 论文；(2) 'CNCF 毕业'不是随便说，要能说出这是 CNCF 的最高成熟度等级（sandbox→incubating→graduated），与 OpenTelemetry 同属 CNCF 追踪生态；(3) 区分清楚——OpenTelemetry 是'采集标准/SDK + Collector'，Jaeger 是'后端'（存 trace + 提供 UI/查询）。不要把 Jaeger 说成是采集器。
- ✅ **Jaeger 解决的核心问题：在一个请求穿过几十个微服务的架构里，定位请求慢/错具体发生在哪一跳（哪个服务、哪次调用）。**
  - 证据：Jaeger 官方文档（jaegertracing.io）：'Jaeger maps the flow of requests and data as they traverse a distributed system, supporting calls across multiple services'。Elastic '3 pillars of observability'：traces 提供 request-scoped context（'where' in the request path），而 logs 只给 'what'、metrics 只给 'how much'。
  - 面试追问：'为什么 metrics + logs 不够，还要 trace？'答——metrics 告诉你'平均延迟 p99 变高了'但不知道是谁拖累的；logs 分散在每个服务、没有跨服务因果链；trace 用同一 trace-id 把一个请求在所有服务的所有 span 串成一条因果链，能直接看到'总耗时里 A 服务占了 800ms，其中调用 B 又占了 750ms'，精确定位瓶颈/错误在哪一跳。
- ✅ **Trace = 由一组 span 通过 parent/child 关系组成的调用树，代表一个请求在整个系统里的完整路径。**
  - 证据：Jaeger 官方架构文档（Architecture 页）：'Spans may be nested via parent-child relationships to form a trace'。Logz.io 'What is Distributed Tracing'：parent span 带多个 children，串行或并行，形成树结构。
  - 面试追问：trace 是树/DAG 还是链表？答是树（一条 trace 有一个根/root span，可以有多个并行 child span），严格说是有向无环图但工程上按树理解即可。追问'一个 trace 怎么唯一标识'——靠 trace-id（W3C 里 16 字节/32 hex）。本项目 read_traces.py 里 df.set_index(['trace_id','span_id']) 正是按 trace_id 聚合一条 trace 的所有 span。
- ✅ **Span = 一个逻辑工作单元，至少包含 operation name、start time、duration；可通过 parent/child 嵌套。**
  - 证据：Jaeger 官方架构文档：'A span represents a logical unit of work that has an operation name, the start time of the operation, and the duration.' 本项目 read_traces.py 用到 operation_name、duration 字段（translate(row['operation_name'])），nl2traces._distill_all_services_traces_pre_agent_run 遍历 trace_data['spans']。
  - 面试追问 'span 之间靠什么建立 parent/child'——每个 span 有自己的 span-id 和 parent-span-id（指向父 span 的 span-id），根 span 的 parent 为空/ROOT。本项目 read_traces._summarize_traces_deterministic 正是靠 row['parent_span'] 一路向上回溯找根，并能检测环（'Cycle detected in trace ... at span ...'）。追问 'operation name 例子'——如 'HTTP GET /user'、'rpc.method'。
- ✅ **Span context = 跨进程传播的 trace 标识，含 trace-id、span-id、采样标志（trace-flags），是让不同服务的 span 能串成一条 trace 的关键载体。**
  - 证据：W3C trace-context 规范定义 traceparent header 携带 version/trace-id/parent-id/trace-flags。OpenTelemetry 'Traces' 概念文档：Tracer 创建 span，span context 用于跨边界传播。
  - 面试追问 'span context 和 span 的区别'——span 是实际的工作记录（含 duration/tags/logs），span context 是其中可被注入到 HTTP header / RPC metadata 用于传播的那一小段标识信息（trace-id + span-id + flags）。传播失败的后果见下面 propagation 那条。
- ✅ **tags 和 logs 是 span 的两类标注：tags 是 key:value 键值对（适合过滤/查询），logs 是带时间戳的事件（适合调试）。**
  - 证据：LogicMonitor 'What Are Spans'：'Span tags annotate general metadata for querying, while logs capture time-specific events. Tags are better for filtering traces; logs are better for debugging.' Thoughtworks 'OpenTracing and Jaeger'：spans accept key:value tags and timestamped logs。本项目 nl2traces.py 逐个检查 span['tags'] 里的 key（http.status_code==500 / error==true / otel.status_code==ERROR / rpc.grpc.status_code==14）。
  - 面试追问 'tags vs logs 选哪个'——要持久化、要能在 UI 上过滤的属性放 tags（如 http.status_code、error=true）；某个时刻发生的事放 logs（如异常堆栈打点的时刻）。追问 '怎么标记一个 span 是 error'——常见 tag：error=true、http.status_code>=500、otel.status_code=ERROR、rpc.grpc.status_code 非 0。本项目正是用这套 tag 判定 has_error。
- ✅ **Trace context 跨进程透传靠 HTTP header：OpenTelemetry 默认用 W3C TraceContext 的 traceparent header（格式 version-traceid-parentid-traceflags，如 00-0af7651916cd43dd8448eb211c80319c-b7ad6b7169203331-01）；B3（X-B3-TraceId 等）是 legacy 格式，主要为了兼容 Zipkin。**
  - 证据：OpenTelemetry 'Context propagation' 文档：默认 propagator 用 W3C TraceContext 规范的 header。W3C trace-context 规范：traceparent 四字段 version/trace-id/parent-id/trace-flags，trace-id 32 hex、parent-id 16 hex、trace-flags 2 hex（01=sampled,00=not sampled）。OneUptime B3 propagation 指南：B3 用于 Zipkin 兼容。
  - 面试高频追问，务必背下 traceparent 格式：version(2 hex)-traceid(32 hex)-parentid(16 hex)-traceflags(2 hex)。'为什么微服务能串成一条链'——入口服务生成 trace-id，每次出站调用把当前 span 的 context 注入 header，下游服务收到后用 parent-id 作为自己 span 的 parent，于是同一 trace-id 贯穿所有服务。追问 'context propagation 失败会怎样'——下游服务拿不到父 context，只能另起一个 trace-id，于是原本一条请求被拆成几段互不关联的 trace，调用链断裂，定位根因会失败（这是生产中最常见的'为什么我的 trace 只有半截'的根因）。追问 'Istio/service mesh 怎么办'——mesh 层自动注入/转发 W3C/B3 header，但存在 header 未被正确透传的已知坑（Istio issue #49557）。
- ✅ **采样（sampling）原因：生产环境请求量巨大，全量采集 span 的存储/网络成本不可接受，所以要只采一部分 trace。分 head-based（入口就决定）和 tail-based（等 trace 完整后再决定）。**
  - 证据：OpenTelemetry 官方 'Sampling' 文档：head sampling 在 trace 开始时决定；tail sampling 在看过 trace 的全部或大部分 span 后决定，可基于错误/延迟过滤。New Relic 博客：tail-based sampling 能基于 errors、latency 过滤，这是 head-based 做不到的。
  - 面试必问权衡（trade-off）：head-based（如 probabilistic / parent-based sampler）在根 span 就决定采样，开销小、且采样决定随 context 传播一致，但'盲采'会随机漏掉错误/慢请求；tail-based（OTel Collector 的 tail sampling processor）在 collector 上缓冲整条 trace 再按规则（如 error=true 必采、延迟>阈值必采）决定，能保证'坏 trace 不丢'但要在 collector 上 hold 住所有 span、内存/延迟开销大，通常配合 head-based 先砍一部分再 tail 精筛。追问 '为啥不 100% 采'——量太大、存储贵、且绝大多数正常 trace 没诊断价值。
- ✅ **Jaeger 的持久化存储后端：主要有 Cassandra、Elasticsearch、OpenSearch；Jaeger 官方更推荐 Elasticsearch/OpenSearch 而非 Cassandra（Cassandra 是 KV 存储，不太契合 Jaeger 的查询模式）。**
  - 证据：Jaeger 官方 'Storage Backends' 文档：Cassandra、Elasticsearch、OpenSearch 是主要支持的分布式存储后端。Jaeger FAQ：'Jaeger team recommends OpenSearch/Elasticsearch over Cassandra'，因 Cassandra 是 key-value store，对 Jaeger 的查询模式不友好。另有 in-memory（仅测试用）和 badger（单节点）。
  - 面试追问 'Jaeger 数据存哪'——能说出 Cassandra / Elasticsearch / OpenSearch 三选一即合格。可补充：v2 之后官方更倾向 OpenSearch（Elasticsearch 改 license 后 AWS fork 出 OpenSearch，Jaeger 加了 OpenSearch 支持，见 issue #3044）。追问 'pricing'——Jaeger 本身开源免费，成本在存储集群 + 摄入/采样开销上；商用 SaaS 版（如 Jaeger as managed service）按摄入量/存储计费。本项目是 self-managed：用 in-memory / 自建存储做实验。
- ✅ **Jaeger v2（2024-11 发布）把 Jaeger 构建在 OpenTelemetry Collector 框架之上，原生支持 OTLP 摄入，消除了 OTLP↔Jaeger 的协议翻译层。**
  - 证据：CNCF 博客 'Jaeger v2 released: OpenTelemetry in the core!'（2024-11-12）：v2 以 OpenTelemetry Collector 框架为基础重构 Jaeger 组件。CNCF 博客 'How Jaeger is evolving to trace AI agents with OpenTelemetry'（2026-05）：原生 OTLP 摄入消除中间翻译步骤。
  - 面试追问 'Jaeger 和 OpenTelemetry 什么关系'——OTel 是采集标准（API/SDK + OTLP 协议 + Collector），Jaeger 是后端（存 + 查 + UI）。v1 时代 Jaeger 有自己的 client/agent/collector；v2 时代直接复用 OTel Collector，统一了追踪 pipeline。区分版本能加分，但不要臆造具体版本号——已知 v2 在 2024 年 11 月发布即可。
- ✅ **在本项目里 Jaeger 是'分布式 Trace'数据通路的落地：DeathStarBench 微服务被注入故障 → 业务请求链路里某 span 变慢/报错 → NL2Traces 把'哪个服务/哪一跳/trace id'翻译成 Jaeger 查询 → 给 Diagnosis agent 定位根因。**
  - 证据：nl2traces.py：NL2TracesCustomTool 描述 'turn it into function arguments... for gathering traces from jaeger'；_generate_jaeger_query 用 LLM 把自然语言 + in-context examples 生成 fd_query_jaeger_traces 调用；_query_jaeger_traces 请求 {grafana_url}/jaeger/api/traces，参数含 service/operation/start/end/limit/tags(error=true)；_summarize_traces 让 LLM 输出 'SERVICE NAME / SUMMARY' 和 root cause。jaeger_linter.py 校验 service/operation/时间窗(<=12h)/limit(1-10)。read_traces.py 的 _summarize_traces_deterministic 靠 parent_span 回溯到 has_error 的叶子 span，输出 fault_services（service + operation）。
  - 面试讲项目时的关键链路：(1) 查询入口是 /jaeger/api/traces（走 Grafana 代理），参数能按 service/operation/时间窗/error tag 过滤；(2) 错误判定不靠单一路径，本项目同时认四种 error tag（http.status_code==500、error==true、otel.status_code==ERROR、rpc.grpc.status_code==14），因为 OTel instrumentation 和不同协议（HTTP/gRPC）打的错误 tag 不一样；(3) 根因定位不是'第一个报错的 span'而是沿着 parent_span 回溯找最深层(has_error 且无 child)的叶子节点——本项目注释明确写'these services may not be the exact place of the root cause, so you may need to dig deeper'，体现对'分布式系统里错误会向上冒泡'的理解；(4) NL2Traces 之上还有 JaegerLinter 做 guardrail（时间窗、limit、service/operation 名校验），防止 LLM 生成无效查询。追问 'Grafana 和 Jaeger 什么关系'——这里 Grafana 作为数据源代理/统一入口把 Jaeger API 暴露在 /jaeger/api 下。
- ✅ **可观测性三支柱：metrics（多少，如 QPS/延迟）、logs（发生了什么，离散事件）、traces（在哪一跳，请求级因果链），三者互补。**
  - 证据：Elastic '3 pillars of observability'：metrics/logs/traces 是基础三支柱。Medium 'Day 2 — Logs, Metrics, and Traces'：Logs=What happened，Metrics=How much，Traces=Where（in request path），tracing 因微服务兴起而必要。Sematext 术语表：traces 比 logs 更能深入分布式系统但 less adaptive。
  - 面试必背八股。追问 'metrics/logs/traces 三选一怎么排优先级'——没有绝对顺序：metrics 适合告警和大盘监控（便宜、聚合）；logs 适合事后深挖细节；traces 适合跨服务根因定位（贵，需采样）。追问 'OpenTelemetry 三支柱都覆盖吗'——是，OTel 现在统一了 metrics/logs/traces 三种 signal 的采集标准，但 Jaeger 只管 traces 这一种 signal 的后端。追问 '为什么需要分布式追踪'——见前述'一个请求穿几十个微服务'那条。
