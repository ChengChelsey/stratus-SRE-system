# 面试备考 · 关键算法公式速记（可背诵可理解）

> 口径：把简历里和面试岗位强相关的算法（Transformer 核心、LoRA/QLoRA、LinUCB）拆成「公式 + 一句话讲清为什么」。面试手写白板或口述都能用。公式用 LaTeX 写（`$...$` 行内、`$$...$$` 独立块），Obsidian / VSCode 都可直接渲染。
> 记忆法：每个公式先记「一句话在做什么」，再记符号；追问「为什么」的部分单独标了 **【为什么】**。

---

## 一、Transformer 核心（必考，能默写）

### 1. 缩放点积注意力（Scaled Dot-Product Attention）—— 整个 Transformer 的心脏

三个输入：查询 $Q$、键 $K$、值 $V$；$d_k$ 是 $K$ 的维度。

$$
\text{Attention}(\mathbf{Q}, \mathbf{K}, \mathbf{V}) = \text{softmax}\!\left(\frac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d_k}}\right)\mathbf{V}
$$

一句话：用 $Q$ 和每个 $K$ 算点积得到「相似度分数」，除以 $\sqrt{d_k}$ 压一下，softmax 归一化成权重，再用权重对 $V$ 加权求和。

拆三步背：
1. $\mathbf{Q}\mathbf{K}^\top$：每个 query 对每个 key 的原始分数（点积 = 相似度）。
2. $/\sqrt{d_k}$：缩放，防分数过大。
3. $\text{softmax} \to \times \mathbf{V}$：归一化成权重，再按权重取值。

### 2. **【为什么】** 要除以 $\sqrt{d_k}$（高频追问）

点积是 $d_k$ 个分量相加，方差正比于 $d_k$。$d_k$ 大时点积值很大，softmax 会落进饱和区、梯度趋近 0，训练不动。$\sqrt{d_k}$ 正是 $d_k$ 的标准差，除掉它等于把方差归一到 1 量级，让 softmax 停在敏感区、梯度健康。

### 3. Q / K / V 哪来的

输入序列每个 token 先投影出三种角色：

$$
\mathbf{Q} = \mathbf{X}\mathbf{W}_Q,\quad \mathbf{K} = \mathbf{X}\mathbf{W}_K,\quad \mathbf{V} = \mathbf{X}\mathbf{W}_V
$$

$\mathbf{W}_Q / \mathbf{W}_K / \mathbf{W}_V$ 是三个可学习权重矩阵，同一个输入 $\mathbf{X}$ 投出查询、键、值三种表示。

### 4. 多头注意力（Multi-Head Attention）

把 $Q/K/V$ 拆成 $h$ 份，每份维度 $d_k = d_{model}/h$，各自做一次注意力，再拼回去做一次线性映射：

$$
\text{head}_i = \text{Attention}(\mathbf{Q}\mathbf{W}_Q^i,\ \mathbf{K}\mathbf{W}_K^i,\ \mathbf{V}\mathbf{W}_V^i)
$$

$$
\text{MultiHead} = \text{Concat}(\text{head}_1, \dots, \text{head}_h)\,\mathbf{W}_O
$$

**【为什么多头】** 让模型在多个不同子空间里学不同的关注关系，比单头一次看更丰富。

### 5. 位置编码（Positional Encoding）—— **【为什么】** 需要

Attention 本身是集合操作，对输入顺序无感（打乱 token 顺序结果不变）。但语言顺序有意义，所以要额外把位置信息加进去。

原版 Transformer 用正弦/余弦：

$$
\text{PE}(pos, 2i) = \sin\!\left(\frac{pos}{10000^{\,2i/d_{model}}}\right)
$$

$$
\text{PE}(pos, 2i+1) = \cos\!\left(\frac{pos}{10000^{\,2i/d_{model}}}\right)
$$

输入 = 词嵌入 + 位置编码：$\mathbf{X} = \text{Embedding}(token) + \text{PE}(pos)$。
（现在大模型多用 **RoPE 旋转位置编码**，把位置编进 $Q/K$ 的旋转里，外推到更长上下文更稳。）

### 6. 前馈网络（FFN）

每个位置独立过两层全连接 + 激活：

$$
\text{FFN}(\mathbf{x}) = \max(0,\ \mathbf{x}\mathbf{W}_1 + \mathbf{b}_1)\mathbf{W}_2 + \mathbf{b}_2
$$

现在常用 GELU / SwiGLU 替代 ReLU。分工记法：**注意力负责 token 之间交换信息，FFN 负责每个 token 内部做非线性变换**。

### 7. 残差连接 + LayerNorm（每个子层的标配）

$$
\text{Post-Norm（原版）：} \text{output} = \text{LayerNorm}\bigl(\mathbf{x} + \text{Sublayer}(\mathbf{x})\bigr)
$$

$$
\text{Pre-Norm（主流）：} \text{output} = \mathbf{x} + \text{Sublayer}\bigl(\text{LayerNorm}(\mathbf{x})\bigr)
$$

- **残差**：解决深网络梯度消失，让信息能直通到底。
- **LayerNorm**：对每个样本的特征维归一化（减均值、除标准差），稳定训练。
- 现在主流大模型几乎都用 **Pre-Norm**（训练更稳）。

### 8. Softmax（反复用到）

$$
\text{softmax}(z)_i = \frac{\exp(z_i)}{\sum_j \exp(z_j)}
$$

把一组实数变成和为 1 的概率分布。带温度 $\tau$ 时是 $\text{softmax}(z/\tau)$：$\tau$ 越大分布越平、越小越尖锐（采样调温度就是调这个）。

### 9. Encoder vs Decoder + 因果掩码（Causal Mask）

- **Encoder**（BERT 类）：自注意力是双向的，每个 token 能看到所有 token。
- **Decoder**（GPT 类）：带因果掩码的自注意力——预测第 $t$ 个 token 时只能看前 $t$ 个，未来位置在 softmax 前置 $-\infty$（mask 成 0），保证自回归不偷看未来。
- **大模型（GPT/Qwen/Llama）都是 Decoder-only + 因果掩码 + 自回归**。

### 10. 推理优化：KV Cache（Agent Infra 岗位很可能问）

自回归生成时，历史 token 的 $K$、$V$ 每步都不变。把它们缓存下来，下一步只算新 token 的 $Q$ 去查历史 KV：第 $t$ 步只需 $\mathbf{q}_t$ 与已缓存的 $[\mathbf{k}_1\dots\mathbf{k}_t,\ \mathbf{v}_1\dots\mathbf{v}_t]$ 做注意力，省掉「重复重算历史 $K$、$V$」的开销。

**代价**：KV cache 显存随序列长度线性增长（长 agent 轨迹/长上下文会爆显存），于是有 **PagedAttention**（分页管理 KV，vLLM 核心）、KV 量化等优化。

---

## 二、LoRA / QLoRA（简历直接写了，必须会）

### 1. 核心思想：低秩增量

预训练权重 $\mathbf{W}_0 \in \mathbb{R}^{d\times k}$ 冻住不动，旁边加一个低秩更新：

$$
\mathbf{W} = \mathbf{W}_0 + \Delta\mathbf{W} = \mathbf{W}_0 + \mathbf{B}\mathbf{A}
$$

$$
\mathbf{A} \in \mathbb{R}^{r\times k},\quad \mathbf{B} \in \mathbb{R}^{d\times r},\quad r \ll \min(d,k)
$$

只训练 $\mathbf{A}$、$\mathbf{B}$。初始化：$\mathbf{A}$ 随机高斯、$\mathbf{B}$ 初始化为 0，所以起步时 $\mathbf{B}\mathbf{A}=0$，模型行为不变，平稳开始学。

### 2. 前向：先 A 后 B 省计算

$$
\mathbf{h} = \mathbf{W}_0\mathbf{x} + \Delta\mathbf{W}\,\mathbf{x} = \mathbf{W}_0\mathbf{x} + \mathbf{B}(\mathbf{A}\mathbf{x})
$$

不把 $\mathbf{B}\mathbf{A}$ 显式乘出来（那是个 $d\times k$ 大矩阵），而是先算 $\mathbf{A}\mathbf{x}$（$r$ 维）、再算 $\mathbf{B}(\cdot)$（$d$ 维）。计算量从 $O(d\cdot k)$ 降到 $O\bigl(r(d+k)\bigr)$。

### 3. 参数量对比（省在哪）
- 全参微调：更新整个 $\mathbf{W}_0$，参数量 $d\cdot k$。
- LoRA：只更新 $\mathbf{A}+\mathbf{B}$，参数量 $r(d+k)$。
- 比例 $\approx \dfrac{r(d+k)}{d\cdot k}$。当 $r \ll d,k$ 时，通常只有原参数量的 0.1%~1%。

### 4. 缩放系数 scaling = α / r —— **【为什么】** α 通常是 r 的 2 倍

真正加进去的增量是：

$$
\mathbf{h} = \mathbf{W}_0\mathbf{x} + \frac{\alpha}{r}\,\mathbf{B}(\mathbf{A}\mathbf{x})
$$

- **为什么除以 $r$**：$r$ 变大时 $\mathbf{B}\mathbf{A}$ 整体幅度会自然变大（更多维度叠加），用 $\alpha/r$ 把它拉回固定量级，这样换不同 $r$ 时不用重新调学习率，$\alpha$ 和 $r$ 解耦。
- **为什么 $\alpha = 2r$**：这是被验证好用的经验默认（你简历 $r=16,\ \alpha=32$；常见还有 $r=8,\ \alpha=16$）。相当于增量权重固定在 2 倍，让低秩增量有足够表达力又不喧宾夺主。

### 5. 秩 r 怎么选

$r$ 越大表达力越强、越接近全参但参数越多；$r$ 越小越省但可能欠拟合。常见 $r = 8 / 16 / 32$。任务越复杂、和预训练分布越远，$r$ 往往要调大。你简历用的是 **$r=16$**。

### 6. QLoRA：在 LoRA 基础上把冻住的 W₀ 量化到 4 比特

QLoRA 三件套：
1. **NF4 量化（Normal Float 4 比特）**：为「正态分布的权重」专门设计的 4 比特量化格点（信息论最优分位点），比均匀量化损失小。前向时反量化回高精度（BF16）算，梯度只走 LoRA 的 $\mathbf{A}$、$\mathbf{B}$（保持高精度），$\mathbf{W}_0$ 不更新。
2. **双重量化（Double Quantization）**：连量化时存的「缩放常数」本身也再量化一次，进一步省显存。
3. **分页优化器（Paged Optimizer）**：优化器状态在显存吃紧时能换页到 CPU 内存，避免 OOM。

### 7. **【为什么】** 4 比特量化既省显存又不掉太多效果
- **省**：权重从 16 比特压到 4 比特，显存降到约 1/4，7B 模型能在单张消费级显卡上微调。
- **不掉太多**：① NF4 按权重正态分布最优设计，量化误差小；② 真正要学的任务适配集中在低秩增量 $\mathbf{A}$、$\mathbf{B}$ 上，它们保持高精度；③ 冻住的大权重只提供通用能力，4 比特重建误差对「微调后任务表现」影响有限。所以效果接近全参，显存省一个量级。

### 8. LoRA vs 全参微调（一句话）
全参更新整个权重，上限略高但显存巨大、易过拟合、每个任务存一份完整权重；LoRA 只存小小的 $\mathbf{A}$、$\mathbf{B}$，一个基础模型可挂多个任务适配器，切换便宜、显存小、效果接近。SRE 规划这种「格式/能力对齐」任务，QLoRA 完全够用。

---

## 三、LinUCB（上下文老虎机，简历写了，必须会更新公式）

### 1. 问题设定：上下文多臂老虎机

有若干候选动作（「臂」，比如若干种缓解策略）。每次看到一个上下文 $\mathbf{x}$（故障特征向量），选一个臂，环境给奖励 $r$。目标：在「试新的探索」和「用已知最好的利用」之间权衡，最大化累计奖励。

### 2. 核心假设：线性收益

每个臂 $a$ 的期望奖励是上下文的线性函数：

$$
\mathbb{E}[r \mid \mathbf{x}, a] = \boldsymbol{\theta}_a^\top \mathbf{x}
$$

$\boldsymbol{\theta}_a$ 是臂 $a$ 自己的未知参数（每个臂独立学一组）。

### 3. 用岭回归估 θ_a

$$
\hat{\boldsymbol{\theta}}_a = \mathbf{A}_a^{-1}\mathbf{b}_a
$$

$$
\mathbf{A}_a = \sum_t \mathbf{x}_t\mathbf{x}_t^\top + \lambda\mathbf{I} \quad (d\times d)
$$

$$
\mathbf{b}_a = \sum_t r_t\,\mathbf{x}_t \quad (d\times 1)
$$

每来一条反馈就往里累加。

### 4. 选臂：置信上界（UCB）—— 探索项的来源

$$
\text{score}_a = \hat{\boldsymbol{\theta}}_a^\top \mathbf{x} + \alpha\sqrt{\mathbf{x}^\top \mathbf{A}_a^{-1}\mathbf{x}}
$$

$$
a^* = \arg\max_a\ \text{score}_a
$$

- **第一项 $\hat{\boldsymbol{\theta}}_a^\top \mathbf{x}$**：利用项，越觉得这个臂好分越高。
- **第二项 $\alpha\sqrt{\mathbf{x}^\top \mathbf{A}_a^{-1}\mathbf{x}}$**：探索项。$\mathbf{x}^\top \mathbf{A}_a^{-1}\mathbf{x}$ 是这个臂在这个上下文下的不确定性——被试得越少，$\mathbf{A}_a$ 这个方向信息越少、$\mathbf{A}_a^{-1}$ 越大，不确定性越高，越鼓励去试。
- **直觉**：又好又不确定的臂，得分被抬上去，值得一试。

### 5. 拿到奖励后更新

$$
\mathbf{A}_a \leftarrow \mathbf{A}_a + \mathbf{x}\mathbf{x}^\top
$$

$$
\mathbf{b}_a \leftarrow \mathbf{b}_a + r\,\mathbf{x}
$$

被选中的臂拿到真实奖励 $r$ 后，把这次的外积和奖励加权上下文累加进去；信息变多 → 下次不确定性降低 → 探索项变小 → 逐渐收敛到最优。

### 6. **【为什么】** α 是干嘛的

$\alpha$ 控制「探索 vs 利用」。$\alpha$ 大→更爱试新（探索激进，可能发现更好的，但前期选次优）；$\alpha$ 小→更信当前估计（利用为主，收敛快但可能锁死次优）。$\alpha$ 是要调的超参。你简历做了 alpha 网格搜，最优是 **$1.0$**。

### 7. 和监督学习 / A-B 测试 / 强化学习 比（高频追问）
- **vs 监督学习**：监督学习有固定标签、离线学完就用；老虎机没有固定标签，奖励是「选了之后才有」，必须在边选边学的反馈回路里更新，核心难点是探索-利用权衡。
- **vs A/B 测试**：A/B 固定分流量、长期跑出统计显著；老虎机边跑边把流量往表现好的臂倾斜，累计收益更高、收敛更快（少在差的臂上浪费）。
- **vs 强化学习**：RL 要建模状态转移、长程回报（多步决策），样本需求大、训练不稳；上下文老虎机是「无状态单步决策」（每次上下文独立、只看即时奖励），简单稳定、样本效率高。SRE「给一个故障选一个缓解动作」天然是单步上下文决策，没必要上完整 RL。

### 8. 离线重放（你怎么评估的）
在线要真去线上交互才有奖励，成本高、有风险。离线重放：用已收集好的固定轨迹数据（每条记了上下文 $\mathbf{x}$、选的臂、奖励 $r$），反复跑算法、统计平均收益，估计策略好不好，不真正线上交互。
**局限**：离线奖励基于历史数据估，能否代表线上取决于数据覆盖度和分布；策略若总选历史没试过的臂，离线估不准。所以离线先筛策略、定超参，上线后再小流量在线验证。

---

## 附：白板默写清单（面试前 5 分钟过一遍）

1. **Attention**：$\text{softmax}\!\left(\dfrac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d_k}}\right)\mathbf{V}$ —— 记住「点积打分 → 缩放 → 归一化 → 加权取值」。
2. **为什么 $\sqrt{d_k}$**：方差正比 $d_k$，不缩放 softmax 饱和、梯度消失。
3. **MultiHead**：拆 $h$ 份各自 attend → concat → 线性。
4. **LoRA**：$\mathbf{W}_0 + \mathbf{B}\mathbf{A}$，先 A 后 B；缩放 $\alpha/r$；$\alpha=2r$ 是经验默认。
5. **QLoRA 三件套**：NF4 量化 + 双重量化 + 分页优化器。
6. **LinUCB 选臂**：$\hat{\boldsymbol{\theta}}^\top \mathbf{x} + \alpha\sqrt{\mathbf{x}^\top \mathbf{A}^{-1}\mathbf{x}}$，第一项利用、第二项探索。
7. **LinUCB 更新**：$\mathbf{A} \mathrel{+}= \mathbf{x}\mathbf{x}^\top$，$\mathbf{b} \mathrel{+}= r\,\mathbf{x}$。
8. **KV Cache**：历史 $K$、$V$ 缓存，每步只算新 $Q$；长上下文显存线性涨 → PagedAttention。
